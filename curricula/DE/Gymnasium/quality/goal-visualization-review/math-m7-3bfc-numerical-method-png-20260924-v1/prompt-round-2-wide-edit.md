Edit this exact existing PNG minimally; preserve the same warm friendly comic composition, characters, colors, bars, correct numbers and all other correctly written German text. Critical semantic clarification: make the central light-blue example label clearly say on two readable lines, exactly: “Beispiel: Intervallhalbierung” and “f(x) = x² − 2”. The four side cards must clearly refer to THIS interval-halving example, not claim all numerical methods converge under the same conditions. In the top-left card use exactly “Konvergenz?” then “Hier: Stetigkeit +” then “Vorzeichenwechsel prüfen”. In the top-right card use exactly “Abbruch:” then “Intervallbreite < 0,01”. Keep bottom/right cards as they are, including “Verfahren scheitert ≠ Lösung fehlt”. Remove the extra taped note saying “GUTE NÄHERUNG = ZUVERLÄSSIGE AUSSAGE” because it overstates reliability; leave visual whitespace there. Keep `[1; 2]`, `[1,4140625; 1,421875]`, and “Breite = 0,0078125 < 0,01” exactly unchanged and correct. Do not add any new mathematical formula or claim. Preserve high contrast and legibility.
