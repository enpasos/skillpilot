# Informed AI visual review: matrix multiplication, candidate v1

- Review date: 2026-09-13.
- Reviewer: Codex sub-agent `/root/math_matrices_visual_review` (AI).
- Review mode: informed independent visual and mathematical inspection. The reviewer read the correction prompt and parent task, including the parent's preliminary positive assessment, and viewed both actual raster files with `view_image` at original detail. All calculations below were independently repeated. This is not a blind D review, a human approval, a release approval or a replacement for page/context checks.
- Decision: `accepted_pilot_after_regeneration` for the candidate bytes below. No remaining concrete mathematical rejection trigger was found in this inspection.
- Generation provider/tool reported by the producing agent: OpenAI / ChatGPT-Codex image generation, `image_gen`. Exact generator model/version is not exposed and is not asserted.

## Binding and references

- Goal: `304111dd-426b-520b-a275-3fa37da1b0e0`, **Matrizen multiplizieren**.
- Canonical text inspected: `curricula/DE/Gymnasium/canonical/DE_DEU_S_GYM_CANONICAL_MATHEMATIK.de.json`: “Die lernende Person kann Matrizenprodukte berechnen, Dimensionsbedingungen prüfen und typische Fehlerquellen bei der Reihenfolge vermeiden.” Q2, AB1, ordinary atomic goal.
- Existing image and style reference: `app/public/assets/goal-visualizations/mathematik/304111dd-426b-520b-a275-3fa37da1b0e0/304111dd-426b-520b-a275-3fa37da1b0e0.jpg`.
- Existing public image SHA-256 at inspection: `f2c14687ec511ff0a0fe5e96012dcc08f426f2d7b094e7768fd70411237ad7eb`.
- Candidate inspected: `/home/enpasos/.codex/generated_images/01a03a32-5c3d-7122-a394-13d4f26b0054/exec-d6c7d200-3b54-4bb9-8aa7-31b810d77d41.png` (1678 × 937, 1520346 bytes).
- Candidate SHA-256: `add0b17d0ea7d252f99de72a54c4abaa879e40f087f365ccaf2c40f7c2a5f14b`.
- Actual supplied prompt: sibling `304111dd-426b-520b-a275-3fa37da1b0e0.prompt-v1.md`.
- Prompt SHA-256: `4dd13fd538727adbf7b84907e66c487b4050ee745bc7dfb8f2806c9d65ab5ce1`.
- Prompt/reference strategy: use the existing raster as the style/content reference, replacing the malformed factor-definition equality chain and the misleading reverse-order example with fully calculated `AB` and `BA` for the same matrices.
- Policy read: current `AGENTS.md` image section and `docs/concept/skill-graph/atomic-goal-visualizations.md`, including the 13 September 2026 KEEP/provider policy and the human/AI distinction.

## Direct visual and mathematical findings

The original shows a central chain shaped as `A = [A] · B = [B] = C = ...`, which incorrectly joins factor definitions and product expressions as equal objects of different dimensions. Its reverse-order panel places “Dimensionen passen oft nicht!” directly below the displayed 3×2 by 2×3 product even though that specific product is defined. The latter is a misleading example/label pairing, not a valid failure of the displayed dimensions. Replacing the defective image is justified independently of its provider.

The candidate displays the same numeric factors consistently: `A = [1, 2, 0; 3, 1, 2]` and `B = [2, 1; 0, 3; 4, 2]`. The left panel uses detached side labels `A` and `B` beside dimensional diagrams; they do not assert an equation or matrix product. The complete, separate definitions `A = [...]` and `B = [...]` appear in the middle panel. This is a harmless difference from the requested left-panel equals signs and does not repeat the old false equality chain.

The left arrows span exactly three columns/two rows of A and two columns/three rows of B. The visible 2×3 and 3×2 labels, inner-dimension check `3 = 3`, and output dimension 2×2 all agree. The central red highlight covers row 1 of A (`1, 2, 0`); the green highlight covers column 2 of B (`1, 3, 2`). Their arrows lead to the calculation cloud for `c₁₂`, without connecting a wrong row or column.

All four entries of `AB` were independently recalculated:

- `c₁₁ = 1·2 + 2·0 + 0·4 = 2`.
- `c₁₂ = 1·1 + 2·3 + 0·2 = 7`.
- `c₂₁ = 3·2 + 1·0 + 2·4 = 14`.
- `c₂₂ = 3·1 + 1·3 + 2·2 = 10`.

The displayed cloud and `C = A · B = [2, 7; 14, 10]` are correct. The reversed product is defined because `(3×2)(2×3)` has matching inner dimensions 2 and yields 3×3. All nine visible entries of `BA` were independently recalculated:

- Row 1: `2·1+1·3=5`, `2·2+1·1=5`, `2·0+1·2=2`.
- Row 2: `0·1+3·3=9`, `0·2+3·1=3`, `0·0+3·2=6`.
- Row 3: `4·1+2·3=10`, `4·2+2·1=10`, `4·0+2·2=4`.

These match `B · A = [5, 5, 2; 9, 3, 6; 10, 10, 4]` and the 3×3 label. A separate local matrix multiplication calculation reproduced both products. The note comparing the two output sizes is correct for these same factors. The warning against entrywise multiplication is appropriate for the standard matrix product. Every displayed matrix has the indicated row/column count and complete enclosing brackets; no invalid product or false equality remains visible.

The image retains the original pale-blue background, three-stage progression, hand-drawn lettering, blue/orange factor frames, green result frame and small caution sign. The formulas have more room and remain readable at the inspected raster size, including the `c₁₂` subscript and every 3×3 entry. No clipped expression, technical ID, watermark or visible provider artifact was found. The three panels directly cover the canonical goal's calculation, dimension and order aspects without adding a new topic.

## Limits and handoff

This receipt approves only the inspected candidate as informed AI pilot evidence. It does not write or imply `humanApproved`, blind D approval, real-host acceptance, licensing approval, current cockpit-card readability or goal-book page acceptance. After import, compare the deployed bytes to this candidate hash and run the affected goal/page/context/evidence checks; a hash refresh by itself is not that content review.
