# Mathematik M7: Kettenregel und Produktregel – PNG-Bildprüfung

Stand: 23.09.2026. Umfang: ausschließlich die zwei aktuellen atomaren Q2-LK-Ziele `ae5010cc-ea8d-5b14-aa4a-b0f2b5846a75` und `cf48c918-f6c1-5429-8da6-14df43f2f550` in `DE_DEU_S_GYM_CANONICAL_MATHEMATIK.de.json`. Beide waren vor diesem Paket ohne aktives Zielbild als `deferred_provider_limitation` notiert. Die folgenden **KI-Bildentscheidungen** gelten nur für die angegebenen aktuellen PNG-Hashes; sie sind keine menschliche Freigabe, kein Nachweis einer Lernendenleistung und kein Abschluss der abhängigen D-/P-/Seitenprüfung.

Herkunft: Beide Erstfassungen entstanden am 23.09.2026 mit dem eingebauten ChatGPT/Codex-Bildgenerator. Die genaue Modellkennung wurde nicht ausgegeben. Für die Erstfassungen wurden keine Bilder an den Generator übergeben; vorhandene freundliche Mathematikillustrationen wurden nur lokal auf Stileignung angesehen. Die Kettenregel-Fassung v2 ist ein gezielter Bild-Edit ihrer verworfenen Erstfassung mit demselben Werkzeug. Die wortgetreuen Prompts stehen noch im lokalen, ignorierten Kandidatenverzeichnis `tmp/math-m7-next-png-candidates-20260923/`; die für die **aktiven** Bytes verwendeten Prompts sind außerdem unter `visualizations/mathematik/<goalId>/prompt.de.md` gespeichert. Als eigene didaktische Bilder tragen die importierten `resourceLinks` die Lizenz `CC-BY-4.0`; die KI-Herkunft ist für sich weder Rechte- noch Qualitätsbeweis.

| Ziel / Entscheidung | Exakter SHA-256 der aktiven PNG-Datei | Fachliche Original-Pixelprüfung und Grenze |
| --- | --- | --- |
| `ae5010cc-ea8d-5b14-aa4a-b0f2b5846a75` · Kettenregel (LK) · **KEEP v2** | `6c71876d3782952b2e7293092a44f10d536b341f14c46a1b9108079ea8526f8f` | Orange äußere Funktion `g(t)=t³`, blaue innere Funktion `u(x)=x²+1`, darunter `f(x)=(x²+1)³` und `f′(x)=3(x²+1)²·2x`. Die Formel multipliziert korrekt mit der inneren Ableitung `2x`; Exponenten, Variablen, Prime und Rechenzeichen sind groß und lesbar. Die umschließenden Comic-Karten zeigen eine Verkettung statt eines Produkts. Es ist nur ein passendes Beispiel, keine allgemeine Beherrschensprüfung. |
| `cf48c918-f6c1-5429-8da6-14df43f2f550` · Produktregel (LK) · **KEEP** | `e902392ddbcc558b904f9a54d9fb74feaec49d92cdf03989d72f8f8fb12efd12` | Die Faktor-Karten `g(x)=x²` und `h(x)=x+1` werden zu `f(x)=x²(x+1)` verknüpft. Die gezeigte Ableitung `f′(x)=2x(x+1)+x²` enthält **beide addierten** Summanden. Die unabhängige Kontrolle `f(x)=x³+x²`, `f′(x)=3x²+2x` stimmt durch Ausmultiplizieren überein. Beschriftungen sind auf Originalgröße lesbar; die abstrakten, freundlichen Karten passen zur bestehenden Comic-Bildsprache. Ein einzelner Kontrollweg ist keine allgemeine Methodenfreigabe oder Lernendenleistung. |

**Nicht freigegeben:** Kettenregel-Erstfassung `candidate.png`, SHA-256 `61b6e0fbc4f1562a3c798b8a9c6cddde0b71904522a3f028cf260045bb94c705`, ist **HOLD**. Sie schrieb im orangefarbenen Außenfeld `außen: [u(x)]³`. Das ist bereits die Verkettung mit eingesetzter innerer Funktion, nicht die unabhängig definierte äußere Funktion. Gerade weil das Lernziel die Kennzeichnung von äußerer und innerer Funktion verlangt, wäre diese Beschriftung didaktisch irreführend, obwohl die darunter stehende Ableitung korrekt ist. Sie blieb ausschließlich im ignorierten Kandidatenordner und wurde nicht importiert. Der Edit-Prompt `prompt-v2.md` verlangte explizit den lokalen Ersatz durch `außen: g(t)=t³`, ohne andere Formeln oder das freundliche Layout zu ändern. Die korrigierte v2 wurde erneut in Originalauflösung geprüft und erst danach importiert.

Beide aktiven PNGs sind 1536 × 1024 Pixel groß. Die Dateipfade `curricula/DE/Gymnasium/visualizations/mathematik/<goalId>/<goalId>.png`, `app/public/assets/goal-visualizations/mathematik/<goalId>/<goalId>.png` und `backend/src/main/resources/static/assets/goal-visualizations/mathematik/<goalId>/<goalId>.png` müssen je Ziel bytegleich sein. Der jeweilige primäre `goal-visualization`-Link im Canonical verwendet den passenden öffentlichen Pfad und beschreibenden Alt-Text; die Bildprüfung steht mit dem exakten Hash und `humanApproved: "no"` in `goal-visualization-qa/mathematik.qa.json`.

Fach- und Bindungsgrenze: Die bestehenden zweisprachigen Zielbeschreibungen und die direkten Q2-LK-Voraussetzungen wurden nicht verändert. Die Bilder behandeln je ein konkretes Polynom-Beispiel; daraus folgt weder die Anwendbarkeit auf jede differenzierbare Verkettung beziehungsweise jedes Produkt noch ein selbstständiger Leistungsnachweis. Der rohe KLP-NRW-`sourceRef` wird hier nicht als verifizierte vollständige Quellendeckung ausgegeben. Die Bildänderungen können bestehende Zielseiten-, Kontext- und P-v2-Fingerprints veralten lassen; diese abhängigen Bindungen sind gezielt fachlich zu prüfen und nicht bloß durch Hash-Nachführung freizugeben. Kein D-/P-Record, keine zentrale Registry und kein historisches Reviewartefakt wurde in diesem V-Paket bearbeitet.
