# Q4 Modellformen: Taxi-PNG-Kandidat (nicht integriert)

Ziel: `07196e72-ba47-54bf-a096-3a79bbb67e23`. Das bisher aktive JPG beschriftet `p·x` unzutreffend als gesamte „Fahrtkosten“, obwohl `K(x)=G+p·x` auch einen Grundpreis enthält. Zusätzlich ist sein Titel enger als die bereits beanspruchte Kompetenz mit Gleichung, Funktion und Ungleichung. Der neue freundliche Comic soll die drei Formen an *einem* korrekt parametrisierten Taxibeispiel zeigen. Er ist noch **keine** Bild- oder M7-Freigabe; alte Assets/Reviews bleiben erhalten.

Die tatsächliche PNG `candidate-v1.png` (1679 × 937 Pixel, SHA-256 `234b2a43b6cbfe7cac257f5d1f574c04a123f2ca49591f2cc53da26d46f0c095`) wurde am 23.09.2026 mit OpenAI/Codex-Bildgenerierung aus dem im `prompt.md` dokumentierten alten JPG als Stil- und Kontextreferenz erzeugt. Ihre unabhängige fachliche Originalbildprüfung steht inzwischen in `review.md`; die Titel-/Textentscheidung, zielgenaue A/M-/D/P-/V-Nachprüfungen und aktive QA-Bindung fehlen noch. Bei einer späteren Prüfung muss besonders kontrolliert werden, dass `2x` nur der streckenabhängige Teil und `K(x)` der gesamte Fahrpreis ist, dass Gleichung `K(x)=20` „genau“, Ungleichung `K(x)≤20` „höchstens“ bedeutet und keine darüber hinausgehende Modellkompetenz behauptet wird.

Die unabhängige v1-Bildprüfung in `review.md` hält den ersten Kandidaten wegen der irreführenden Pfeilkette an. `candidate-v2.png` ist der gezielte Bearbeitungskandidat mit zwei Ästen von der Funktion; der exakte Bearbeitungsprompt, der bekannte Tool-Ursprung und der Hash stehen in `prompt-v2.md`. Für v2 liegt noch **keine unabhängige Freigabe** vor; die Bilddatei ist nicht importiert.
