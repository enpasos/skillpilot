# J5 geometry: independent image audit before current D review (2026-09-24)

Scope: the two existing, hash-bound JPEGs below were inspected at native resolution and at 360 px width against the current canonical DE/EN goals, their current P-v2 evidence profiles, the BY J5 M5.2 source (`input/BY/raw/Gymnasium_Mathematik_Jgst_5.pdf`, “Kompetenzerwartungen und Inhalte”), and their visualization prompts. This is an AI audit, not human approval or a change to the visualization QA ledger.

| Goal / current asset SHA-256 | Finding | Decision |
| --- | --- | --- |
| `2231c29b-eb4e-51ae-9cb1-eb033bf16099` / `ec06d27bbc0cbac5f4ea895d48d353186481de26cfa840b4408c6ced387bab94` | The two left-hand `g ∥ h` examples have matching directions. On the right, both pairs are labelled `k ⟂ l` and carry square right-angle marks, but the blue strokes do **not** form right angles. Approximate full-resolution stroke endpoints for the lower pair are `k: (1806,996)→(2150,1425)` and `l: (1770,1380)→(2290,1107)` in the 2752×1536 image; their undirected acute crossing angle is about **79°**, not 90°. The upper pair is about **85°** by the same endpoint method. These are approximate readings of hand-drawn strokes, not exact line fitting; the lower discrepancy is still clearly visible at 360 px. The prompt requires genuine perpendicular pairs, and the canonical/P-v2 goal makes the right angle the *reason* for the classification. A printed 90° mark cannot repair inconsistent geometry. The existing AI V approval at this same hash says the right-angle symbols are correct, but does not resolve the drawn-angle contradiction. | **HOLD / fix V** before current D closure; inspect a corrected asset at both sizes and re-review its new hash. |
| `25593605-5e13-55cc-9a05-8f3d737e15e9` / `c943a7e83053e4d5e978e2f5365d03c837b6e218edb6f70393eaeee9560f519f` | The central equal-unit grid places `P(−3|2)` three units left/two up and `Q(4|−1)` four right/one down. The left `Strecke PQ` stops at its two marked endpoints; the right `Gerade g` passes straight through its P and Q marks and continues to arrowheads in both directions, with the same down-right direction. At 360 px the point locations and segment-versus-line distinction remain visible; small axis labels are still legible on inspection. This matches the current canonical/P-v2 distinction and BY M5.2's coordinate/line-object requirement. | **KEEP** this image for current D review; no image-level hold found. |

The canonical and public JPEG copies match each listed hash. Both QA entries remain `aiApproved: yes`, `humanApproved: no`; neither QA status nor any canonical, registry, or D record was edited here. The D review must not treat the first image's old AI V status as proof of geometric correctness.
