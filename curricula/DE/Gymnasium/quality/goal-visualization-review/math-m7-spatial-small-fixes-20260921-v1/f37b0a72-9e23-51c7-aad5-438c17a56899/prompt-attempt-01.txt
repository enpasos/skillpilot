Use case: precise-object-edit. Edit the supplied existing German classroom infographic. Preserve its title, friendly blue backdrop, comic arrows, two-panel layout, and ALL correct original numbers: a=(2|1|1), b=(-1|2|3), a+b=(1|3|4), 2a=(4|2|2). Keep both existing component calculations unchanged. Correct only the geometric arrow diagrams and their axes.
Left panel: give the addition its own small x,y,z axes at a clearly marked common origin O, with positive x diagonally down-left, y right, z up. Use one consistent affine projection throughout: one x unit projects (-70,+35), one y unit (+70,0), and one z unit (0,-50) in screen coordinates where down is positive. Thus projected a moves (-70,+20), b moves (+210,-185), and a+b moves (+140,-165). You may multiply ALL these screen displacements by the same scale to fit the panel. Draw blue a from O slightly left and down; draw green b starting EXACTLY at the blue arrowhead and extending up-right; draw red a+b from O to the EXACT final green arrowhead. The red result and green endpoint must coincide. Label arrows with their unchanged triples and keep the head-to-tail meaning obvious. The projected b MUST point up-right, never left. Keep plenty of room around the adjacent correct calculation text.
Right panel: remove the old shared coordinate axes and show a deliberately axis-free direction sketch, labelled "Richtungsskizze". The orange 2a and blue a share EXACTLY one start point and EXACTLY one straight ray. Put the blue a arrowhead at the exact midpoint of the longer orange 2a arrow. Use an orange backing shaft and narrower blue overlay along its first half if needed; do not move the blue arrow to a parallel offset line. Orange 2a has exactly twice the length of blue a. No diverging rays. Keep the labels a=(2,1,1) and 2a=(4,2,2).
Do not display construction pixel values. No extra content, technical IDs, URLs, watermarks or brand names. Keep the existing friendly classroom comic character. Return a landscape PNG.
