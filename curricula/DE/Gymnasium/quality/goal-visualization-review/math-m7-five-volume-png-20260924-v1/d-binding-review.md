# D-Bindung der fünf Volumen-PNGs: aktueller offener Stand

Stand 24.09.2026. Die fünf kanonischen PNGs und ihre Alt-Texte sind fachlich gegen die unveränderten Volumenkompetenzen geprüft. Die älteren D-Resolutionen liefen auf den zuvor aktiven Bild-/Seitenbindungen; sie sind historisches Reviewmaterial, keine Freigabe für die aktuellen PNG-Seiten. Der zentrale D-Owner wurde deshalb auf die vier **unbetroffenen** Ziele seines Neuner-Pakets eingegrenzt.

Die zwei unabhängigen aktuellen D-Runden der Siebenerkampagne `m7-seven-png-current-d-20260924-v1` kamen bei allen fünf Volumenzielen nicht zum selben Urteil: Runde A `REVISE` wegen sichtbar rohem Formel-LaTeX im druckbaren Lernzielbuch-PDF, Runde B `KEEP` für den fachlichen Zieltext und das PNG. Der Web-GoalBook- und Cockpit-Haupttext setzt diese Formeln bereits per KaTeX; der veröffentlichte PDF-Renderer gab sie als Rohtext aus. Der Widerspruch ist real und wird nicht als Konsens umgedeutet. Die Teilresolution der Kampagne schließt nur `b431` und `502`; alle fünf Volumenziele bleiben dort `review_dissent`.

Die bilingualen kanonischen Volumen-Zieltexte wurden daraufhin **ausschließlich in der Formelnotation** von `$...\\...$` auf druckbare Zeichen geändert: Prisma `V = G · h`, Pyramide `V = (G · h)/3`, Zylinder `V = πr²h`, Kegel `V = (πr²h)/3`, Kugel `V = (4/3)πr³`; die expliziten Faktoren `1/3` bleiben erhalten. Die nachprüfbare Vorher-/Nachher-Tabelle samt A-/M-/semantic-kind-/P-Fingerprintfolgen steht in `formula-text-normalization.json`. Keine mathematische Aussage, Ziel-ID, Quelle, Voraussetzung, Bilddatei oder Alt-Text wurde damit geändert. Die fünf tatsächlichen PNG-SHA-256-Werte bleiben `a1b47a51…`, `10fc4e45…`, `cd58c981…`, `cc856d36…`, `25ba62f5…`; die QA bindet sie vollständig, nicht nur diese Kurzformen.

Für einen D-Abschluss sind nun ein neu aus aktuellem Ziel, P-Profil und GoalBook-Seite gebautes Fünfer-Bundle, zwei **neue unabhängige** D-Runden sowie eine native Synthese/Resolution erforderlich. Konfiguration und fachliche Prüfkriterien liegen unter `goal-description-review/mathematik/rollout-v1/2026-09-24/m7-five-volume-unicode-current-d-20260924-v1.*`; das Bundle darf erst nach der gezielten aktuellen P-Neubindung erzeugt werden. Bis dahin ist dies ein dokumentierter offener Grenzfall, keine M7-Zählung und keine menschliche Freigabe.
