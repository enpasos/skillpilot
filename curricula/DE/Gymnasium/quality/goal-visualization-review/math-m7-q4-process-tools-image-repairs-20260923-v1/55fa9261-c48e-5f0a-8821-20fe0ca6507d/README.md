# Bildkorrektur-Kandidat: Minimumwert einer Parabel

Ziel `55fa9261-c48e-5f0a-8821-20fe0ca6507d`.

Der aktive Bestands-JPG mit SHA-256 `d8016bac08699402e5f27e57dea200d66b4e8e3eb9bc7b77b0e4ecbdf773a3ed` ist bytegleich als `original-before-correction.jpg` gesichert. Er bezeichnet `−4` ungenau als „Minimum bei −4“, obwohl `−4` der Funktionswert und `x=0` die Stelle ist. Die Fachkorrektur ändert diese Notiz auf „Minimumwert −4 bei x=0“.

`candidate-v1.png` wurde am 23.09.2026 mit der eingebauten ChatGPT/Codex-Bildbearbeitung aus genau diesem JPG erzeugt. Der Bearbeitungsauftrag steht in `prompt-v1.md`. PNG-SHA-256: `f40de97613e67d9b28f1bbebf393ebf52fd273d6e02f83a28ca78802b79cd8d7`.

Eine unabhängige Prüfung am Originalpixelbild bestätigt die neue Notiz, `f(x)=x²−4`, die Wertetabelle, die Nullstellen `−2` und `2` sowie den Tiefpunkt `(0|−4)` als konsistent. Der freundlich-abstrakte Stil und die Lesbarkeit bleiben erhalten. **Bild fachlich KEEP und mit identischem SHA in kanonisches, öffentliches und Backend-Asset importiert.** Die alten aktiven JPG-Dateien wurden erst nach bytegleicher Archivprüfung entfernt. Die 797-seitige Mathematik-Lernzielbuch-Publikation wurde neu gebaut; Modell und Render-Manifest binden diese PNG, und die Publikation besteht die Integritätsprüfung. Ein separater aktueller P-v2-Nachweis ist nativ 1/1 gültig, bleibt aber `ai_candidate`. Die zwei unabhängigen D-Runden für die neue Seite stehen noch aus; keine menschliche Freigabe.
