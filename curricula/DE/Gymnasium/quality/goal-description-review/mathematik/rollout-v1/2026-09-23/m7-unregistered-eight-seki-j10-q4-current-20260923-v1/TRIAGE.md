# M7 Mathematik: acht noch unregistrierte Ziele für den Erstpass

Stand der Auswahl: 23.09.2026. Der aktuelle bundesweite Mathematik-GoalBook-Stand enthält 797 `curricularAtomic`-Seiten. Gegen alle im zentralen Deep-Understanding-Rollout registrierten D-Resolutionen und P-Konfigurationen sowie alle 15 aktiven In-flight-Batch-Claims abgeglichen, bleiben genau diese acht Seiten ohne D/P-Registrierung und ohne Claim. Der native `select`-Lauf bestätigte die aktuelle Buch- und Ledger-Bindung; für diesen Batch wurde zusätzlich die strengere D-oder-P-Ausschlussregel angewendet. Der Selektor allein schließt nur vollständig abgeschlossene D/P-Schnittmengen aus.

Die acht Ziele sind im gemeinsamen Batch nach Reviewkontext gruppiert:

1. **Bundeslandspezifische Sek-I-Ergänzungen, Geometrie (3):** `0f6c1df6` (GoalBook-Seite 666), `2041f4ec` (670), `d051857c` (672).
2. **Bundeslandspezifische Sek-I-Ergänzungen, Größen und Messen (2):** `2345ae25` (678), `25a3cc39` (680).
3. **Jahrgangsstufe 10, ganzrationale Funktionen (1):** `6a4716bd` (770).
4. **Q4 LK, komplexe Zahlen (2):** `a8fdbaeb` (794), `e105bad8` (795).

Die V-Bestandsaufnahme zum Prepare-Zeitpunkt: Zwei Ziele haben einen verfügbaren Bildlink (`2345ae25`, `6a4716bd`), sechs haben kein Bild. Bei den beiden verfügbaren Bildern ist kein aktuelles hashgebundenes `aiApproved: yes` eingetragen. Die D-Erstrunden dürfen aus Bildverfügbarkeit keine V-Freigabe ableiten. P-Evidence und Bildkorrekturen werden in diesem Vorbereitungsschritt nicht bearbeitet.

Native `prepare` und `check` für die Batch-Konfiguration waren erfolgreich. Das aus dem aktuellen GoalBook abgeleitete Bundle liegt in `bundle/` einschließlich PDF und HTML; die unabhängigen D-Eingaben liegen getrennt in `round-a/` und `round-b/`. Modell-Digest: `sha256:3c22311926a1909942fd55286b01ecfa1866fc9402d8710b5addb621f633d031`; Bundle-Fingerprint: `sha256:982a6aa4d7cee409cc23ae98050ece6983c79bf6780be967f124bbf370ee70d5`. Diese Vorbereitung gewährt keinen M7-Fortschritt, keine fachliche Entscheidung und keine menschliche Freigabe.
