# Mathematik M7: vier aktuelle Bildbindungen, unabhängige D-Reviews

Apply the shared description-understanding-evidence prompt to each goal, using
its current bilingual canonical text, exact source and applicability evidence,
prerequisites, rendered whole page, current image pixels and separately
reviewed positive-understanding-evidence-v2 profile. Record only a candidate
AI judgment; neither an image nor reviewer agreement constitutes human approval.

- Preserve the current competence and course/year applicability. `keep` is the
  default for adequate text. Use `revise` only for an identified local defect;
  `block` for unresolved mathematical, source, identity or scope ambiguity.
- Name the underlying mathematical relationship and independently observable
  learner performance, not a formula lookup, copied diagram or numerical
  substitution. Demand meaningful changed-case transfer while remaining
  method-neutral and age-appropriate. Keep DE and EN equivalent. Leave detailed
  cases, repeated demonstrations and coverage conditions in the P-v2 profile.
- `4d906967…`: a prediction interval starts from a *known* model proportion
  and fixed future sample size; it concerns the range of a future sample
  proportion, not a plausible range for the unknown population proportion.
  The illustration's outside outcome prevents a guarantee interpretation.
- `5c9ac68c…`: a confidence interval uses an *observed* sample to make a
  qualified statement about an unknown, fixed population proportion. Do not
  swap its inferential direction with the prediction interval, or assert that
  the fixed parameter randomly moves between repeated intervals.
- `fc34449a…`: interest duration is the first whole period at which the target
  is reached under the stated compounding model. Check initial value, growth
  factor, target direction and threshold, including the previous period; the
  small illustrated example is not a learner solution or an answer template.
- `5d9c156b…`: a point dividing a segment in a specified ratio must lie on
  the segment at the correct fraction of the vector from A to B. Keep AP:PB
  distinct from the fraction AP:AB, and check orientation when endpoints or
  ratio order change. The five-unit picture is only one representation.

Inspect the original image and an actual mobile-width rendering, plus current
goal/page/context/source bindings. Recheck changed assets substantively; do not
merely refresh hashes. A separate P profile must require independent learner
work in different cases, not reproduction of the picture.
