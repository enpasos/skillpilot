# Mathematik M7: zwei gezielte Open3-Textpräzisierungen

Stand: 2026-09-23. Dies ist eine fachliche KI-Prüfung und Bindungsdokumentation, keine menschliche Freigabe oder D-/M7-Abschlussentscheidung. `f37b0a72-9e23-51c7-aad5-438c17a56899` bleibt als strukturelle Split-Frage offen.

## `4c494716`: geometrische Matrixabbildungen

Die bisherige unqualifizierte Formulierung „Schattenwürfe oder Projektionen“ war für Matrixabbildungen zu weit: Eine Zentralprojektion ist im üblichen Koordinatenraum im Allgemeinen nicht linear. Die Quelle HMKB Q2.5, S. 44, nennt Schattenwürfe als Beispiel; deshalb bleibt das Beispiel erhalten, aber präzise als **Parallelprojektion**. Das tatsächlich betrachtete aktive PNG zeigt parallele Projektionspfeile in Richtung `(1,−1)` und die lineare Abbildung `S(x,y)=(x+y,0)` mit `S(2,3)=(5,0)` und `S(−1,2)=(1,0)`. DE und EN verlangen nun dieselbe einzelne Kompetenz: eine lineare Abbildung mit einer Matrix beschreiben und ihre geometrische Wirkung deuten. Titel, Quelle, AB2, Voraussetzungen, Bilddatei und fachlich korrektes Bild-Alt bleiben unverändert.

## `52e57eb5`: Vier- und Mehrfeldertafeln

„Bedingte Wahrscheinlichkeiten ablesen“ konnte eine Innenzelle fälschlich schon als bedingten Anteil ausgeben. Die präzisierte Beschreibung fordert die passende **Bezugsgruppe**. Das aktive PNG zeigt eine konsistente Vierfeldertafel mit `P(A∩B)=30/100=0,30`, `P(A|B)=30/50=0,60` und `P(B|A)=30/40=0,75`; die beiden Bedingungen haben verschiedene Randgruppen. HMKB Q3.1, S. 46, Spiegelstriche 4 und 5, decken Tabellen und bedingte Wahrscheinlichkeiten. Der AB1-Schwerpunkt bleibt Tafel erstellen und Anteile aus ihr ermitteln; das getrennte AB2-Ziel `c3b9c561-dd83-5903-9ec6-49c7f51bafd5` zur Berechnung bedingter Wahrscheinlichkeiten wird nicht einverleibt. Titel, Quelle, Voraussetzungen, Bilddatei und Bild-Alt bleiben unverändert.

## Genau betroffene aktuelle Belege

- Die kanonischen DE-/EN-Beschreibungen und die zwei spiegelnden `mathematik.qa.json`-Beschreibungen wurden angepasst. Beide vorhandenen PNGs und ihre AI-Bildurteile bleiben inhaltlich gültig; eine neue menschliche Bildfreigabe wird nicht behauptet.
- `mathematik.semantic-kinds.json` wurde für genau diese zwei Zielquellen neu gebunden. Beide bleiben fachlich `curricularAtomic`.
- Die aktuellen Atomaritäts- und Memory-Entscheidungen wurden für beide Ziele mit neuen Fingerprints und individuellen Gründen wiederholt: je `atomic`, je `no_memory_needed`. Die vollständigen nativen A-/M-Checks melden **797/797 aktuell** und keine veralteten Records.
- Die zehn alten positiven Verständnisprofile `canonical-math-positive-understanding-evidence-m7-matrix-probability-ten-20260922-v1` bleiben unverändert. Ihre acht nicht betroffenen JSONL-Zeilen werden byteidentisch durch eine neue 8er-Konfiguration als aktueller Besitzer registriert. Zwei versionierte neue Kandidaten binden die **unveränderten, fachlich weiterhin passenden Transferprofile** nach Prüfung an die präzisierten Ziele und die aktiven PNGs. Beide P-Konfigurationen validieren nativ mit null Blocking Issues. Der Status bleibt ausdrücklich `needs_human_review` / `ai_candidate`, nicht `approved`.
- Die frühere D20-v2-Erstrunde bleibt Historie. Die beiden geänderten Ziel-/Seitenbindungen benötigen eine **neue unabhängige aktuelle D-Doppelrunde** und dürfen nicht allein durch Hash-Austausch als abgeschlossen gelten. Die zugehörigen Visualisierungs-/Kontextbindungen sind nach der Textänderung ebenfalls gezielt neu zu prüfen. Andere 18 Ziele und das offene Split-Ziel erhalten hier keine neue Entscheidung.
