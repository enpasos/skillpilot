# D20 v2 partial description closure: 15 current-page KEEP/KEEP goals

This versioned, batch-local contribution closes **only the D (description) gate** for the 15 current-page goals named in `stable-keepkeep-15-v1.authoring.json`. The prepared 20-goal batch has 16 KEEP/KEEP pairs, but `6fc9246a-9448-4cdb-b627-cf20ea1c65d3` is excluded because its current Page-Fingerprint differs from the D-review binding. The other four excluded goals do not have KEEP/KEEP pairs. Both independent source records remain unchanged `candidate`/`ai_candidate`; the resolutions use `ai_synthesis`, select one concrete bilingual evidence chain per goal, and contain `humanAttestation: null`. The synthesis author was also the prior Round B reviewer, not an independent third or human approver.

The native full-batch index contract v2 requires one resolution for every batch goal, so a 15-of-20 claim cannot truthfully use it. This package uses the existing **legacy schema-v1 partial-group compatibility path** instead. The materializer checks the exact 20-goal dual summary and 16/4 decision partition, then separately excludes 6fc. It rebuilds the current base/subset GoalBook, compares all 20 pages to the prepared batch, proves that only 6fc differs and only by `visualization.qaStatus: review_candidate -> rejected` plus the resulting Page-Fingerprint, and checks each included current page, complete canonical context, goal/page/image digest, authoritative semantic kind, public and canonical image bytes, and original A/B run/context binding. It freshly runs `validateGoalDescriptionDualRoundResolution` for each of the 15 claimed goals and `validateLegacyResolutionIndexSnapshot` for the partial index. Existing output bytes must match on a read-only rerun. The isolated reporter config exercises the production resolution-index loader and all 15 native resolution checks without registering the index centrally.

Run from the repository root:

```bash
app/node_modules/.bin/tsx app/scripts/materializeMathM7D20Stable15PartialResolutions.ts
app/node_modules/.bin/tsx app/scripts/reportDeepUnderstandingRollout.ts --config=curricula/DE/Gymnasium/quality/goal-description-review/mathematik/rollout-v1/2026-09-23/m7-matrix-spatial-repaired-and-scope20-current-v2/stable-keepkeep-15-v1.validation.config.json --mode=check --format=json
```

`6fc9246a-9448-4cdb-b627-cf20ea1c65d3` remains a fachlich KEEP/KEEP **review finding**, but **D/V-HOLD for current closure**. The prepared base/subset digests were `sha256:2a698ea361b513f722cc625cfc52e9f74c451b50c5e24656c4a8a027524dea79` / `sha256:75b11e839792e6543141fe39d86097af9264285d76ee0b96aa40febb57c47fb2`; current rebuilds are `sha256:6af830be9ae935c9605328f1e5509b157581c181b777619918ec763f88a9bf43` / `sha256:a9bd1a00a95aeb14e5f72cbb19e8e56d2cbf3e8035fe676c45bab959e94b172b`. 6fc alone has prepared/current Page-Fingerprints `sha256:12e525a08b070bc1f4c48e98eac73618c404dcc51e6b0cced9bd2117f4773823` / `sha256:3c0fa5b4c4a3165a71b786eb94fcec301b5e90e23a832b21ab258d766b5e01f9`; its Goal-Fingerprint and original JPG bytes are unchanged. In both upper vector rows of that JPG (`sha256:ad5f2098065421f707e8f42637a4c427d5f7ce5fc136fb488d1195bd9b98507d`), same-direction arrows label different coordinate vectors. Current QA has `aiApproved: no` and page `qaStatus: rejected`; an extensively redrawn replacement attempt is also on hold. A corrected image and targeted new page/review binding are needed before 6fc can enter current D closure. This package neither sets nor claims V-yes. Its isolated report deliberately has no positive-evidence profile configurations, so it cannot establish P or M7 completion.

No canonical goal, A/B record, central resolution registry, visualization asset, QA ledger, or human approval is changed by this partial D materialization. The denominator in the legacy index is only its required historical-format snapshot; the central reporter owns live denominators and any future registration/overlap decision.
