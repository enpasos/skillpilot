# Mathematik M7: unabhängige aktuelle D-Prüfung sieben geänderter Bildseiten

Prüfe die **jetzt gerenderten** DE-/EN-Texte und vollständigen Lernzielseiten nach dem gemeinsamen D-v2-Prompt. Diese neue Runde ist nötig, weil die primären Lernzielbilder und dadurch die Seiten-/Kontextfingerprints geändert wurden; frühere D-Entscheidungen und V-Prüfungen sind weder Vorgabe noch Ersatz für dein eigenes Urteil. Lies Bild, Alt-Text, Voraussetzungen, Nachfolger und Quellenkontext je Ziel zusammen. Eine gute Beispielgrafik belegt keine Transferleistung; eine frühere Bildschwäche darf nicht unbesehen dem neuen PNG zugeschrieben werden. Alle Resultate bleiben KI-Kandidaten ohne menschliche Freigabe.

Bei `b431148b…` entscheide, ob der zweisprachige Text tatsächlich eine **begründete Plausibilitätsentscheidung** über eine Normalnäherung verlangt, nicht bloß das Erkennen einer Glockenform. Das Bild vergleicht 100 unabhängige faire Münzwürfe mit 100 unabhängigen Versuchen bei `p=0,01`; rechts ist `np=1` und die Verteilung stark rechtsschief. Prüfe, ob „Normalmodell hier unpassend“ zum Gegenfall passt und ob die Quelle zu annähernd normalverteilten stochastischen Situationen gedeckt bleibt. Eine glockenartige Stichprobenform allein beweist das Modell nicht.

Bei `502ecaa7…` prüfe die Unterscheidung zwischen zulässigen Eingaben aus **Term**, **Graph** und **Sachkontext**. Die drei Bildfelder sind unabhängige Beispiele: `f(x)=1/(x−3)` mit `x≠3`; `y=2` auf `[1,4)` mit geschlossenem/offenem Rand; ein Weg-Zeit-Kontext mit `t≥0`. Stelle nicht fälschlich eine gemeinsame Funktion der drei Felder her. Vergleiche mit HE E.1 „Definitionsmenge“ und dem bestehenden Zieltext, der eine fachsprachliche Begründung verlangt.

Bei `1e77bb2f…` (Prisma) muss `G` eine Grund**fläche** sein, `h` die senkrechte Prismenhöhe und `V=G·h` geometrisch erklärbar. Das neue Bild hat 5 cm × 4 cm Grundseite, `G=20 cm²`, `h=7 cm`, `V=140 cm³` sowie kongruente Querschnitte. Der Text verlangt zusätzlich Skizzen oder Koordinaten und korrekte Einheiten; prüfe, ob die Beschreibung das ohne reine Formelsammlungskompetenz trägt.

Bei `288633c1…` (Pyramide) vergleiche eine quadratische Grundfläche 6 cm × 6 cm und **dieselbe senkrechte Höhe** 9 cm mit dem entsprechenden Prisma. `G=36 cm²`, Prisma `324 cm³`, Pyramide `108 cm³`; die Volumenplättchen sind eine Zahlen-/Volumeneinheiten-Analogie, keine geometrische Zerlegung durch drei Pyramiden. Prüfe den Faktor `1/3` in DE/EN sowie Transfer auf Skizzen/Koordinaten.

Bei `c71ae268…` (Zylinder) führt der Radius 3 cm von Kreismitte zum Rand, nicht über den Durchmesser; die senkrechte Höhe ist 10 cm. `G=9π cm²`, `V=90π cm³`; die gestapelten Scheiben illustrieren konstante Kreisquerschnitte. Prüfe das begriffliche Verständnis der Kreisfläche als Grundfläche und die Volumenbeziehung.

Bei `e8237315…` (Kegel) müssen Kegel und Vergleichszylinder denselben Radius 3 cm und dieselbe senkrechte Höhe 12 cm haben; Mantellinie und Körperhöhe dürfen nicht verwechselt werden. `G=9π cm²`, Zylinder `108π cm³`, Kegel `36π cm³`; prüfe das begründete `1/3`-Verhältnis und den zweisprachigen Text.

Bei `2f2c9f1a…` (Kugel) ist 3 cm bzw. 6 cm der **Radius**, nicht Durchmesser; `V=4/3πr³` ergibt `36π` bzw. `288π cm³` und Verdoppelung des Radius verachtfacht das Volumen. Die Zeichnung ist ein schematischer Größenvergleich, kein 2D-Flächenbeweis. Prüfe, ob Text und Seite die kubische Abhängigkeit, Berechnung und neue Kontexte sauber unterscheiden.

Pro Ziel eine eigene fachliche KEEP/REVISE/SPLIT_REVIEW/BLOCK-Entscheidung mit konkreter Begründung und allen sechs bilingualen Feldern für wesentliches Verständnis, beobachtbare Leistung und Transfer. Keine automatische Übertragung älterer Bewertungen und kein Freigabestatus über `ai_candidate` hinaus.
