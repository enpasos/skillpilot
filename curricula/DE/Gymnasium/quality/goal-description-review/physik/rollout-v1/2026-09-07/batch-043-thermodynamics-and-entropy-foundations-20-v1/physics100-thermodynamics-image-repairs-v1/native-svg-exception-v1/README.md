# Enge SVG-Ausnahme: zwei geprüfte und technisch importierte Kandidaten

Nur 91b20476 und 616ac6cf. Die Ausnahme wurde von root nach eigener vollständiger Rasterprüfung der wiederholt fehlerhaften Nano-Banana-Kandidaten ausdrücklich angefordert. Grundlage ist AGENTS.md:676; dies ändert weder den Standardanbieter noch andere gute Bilder.

Die abgelehnten Versuche mit Bild-SHA, versionierten Promptpfaden, konkreten sichtbaren Defekten und Begründung der lokalen Ausnahme sind in `manifest.json` gebunden. Die zuvor privaten Beobachtungen samt offen dokumentierten, erst durch root aufgedeckten zusätzlichen Fehlern stehen eine Ebene höher in `candidate-observations-after-root-sighting.jsonl`. Keine nachträgliche Behauptung einer früheren vollständigen Fehlererkennung.

Die SVGs sind die deterministischen Quellen, die gleichnamigen PNGs vollständige 1600×900-Sichtprüfungs-Proofs, erzeugt mit `rsvg-convert`. Beide wurden vom Autor vollständig betrachtet; die unabhängige root-Sichtung entscheidet über Weiterverwendung. Es gibt weder Import noch kanonische oder QA-Publikation durch diesen Agenten, keine menschliche Freigabe und keine Quellen-/Projektionsfreigabe.

Nachtrag: Nach vollständiger eigener Nachprüfung und tatsächlicher vollständiger PNG-Gegensichtung durch root wurden genau diese zwei SVG-Quellen auf ausdrückliche Anweisung mit dem nativen Importer übernommen. `import-receipt.json` bindet die informed/nichtblinde AI-Gegensicht, sechs identische Source/Public/Backend-Hashes, kuratierte Rekonstruktionsprompts und die erfolgreiche Eingrenzung auf genau zwei Resource-Link-Änderungen. Der obige Absatz und `manifest.json` beschreiben den früheren Kandidatenstand; dieser Nachtrag ist der spätere Status. Keine QA-Ledgerwrites oder menschliche Freigabe durch diesen Agenten.
