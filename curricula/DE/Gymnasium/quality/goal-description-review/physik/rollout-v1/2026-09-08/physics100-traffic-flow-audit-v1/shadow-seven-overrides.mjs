// Read-only experiment: exact seven B034 additive overrides, no other changes.
import fs from 'node:fs'
import {createHash} from 'node:crypto'
import {pathToFileURL} from 'node:url'
import Module,{createRequire} from 'node:module'
const root=process.cwd(),file=root+'/curricula/DE/Gymnasium/canonical/DE_DEU_S_GYM_CANONICAL_PHYSIK.de.json',bytes=fs.readFileSync(file,'utf8'),c=JSON.parse(bytes),hash=s=>'sha256:'+createHash('sha256').update(s).digest('hex'),ids=['68034218-8f3e-5f7f-ae4e-ed057dd4e44e','69280706-5af6-5788-85b7-93287c2ffe74','7d4d6a39-0c78-5fb0-b7bf-182ed00972f7','922f32ba-f214-5a82-be5c-1111aca51d4a','c2af45aa-e3fc-5119-9159-c5a260b4135a','e1b21fe9-ab6f-58c4-a0b6-9895061fbabb','f505f039-3f07-5afe-88b8-f02532e9c2f0'],read=fs.readFileSync
let virtual=bytes
const cache=new Map(),directories=new Map(),native=root+'/app/scripts/applicabilityCompiler.ts',require=createRequire(native),ts=require('typescript'),vfs={readFileSync(p,encoding){const key=String(p);if(key===file)return encoding?virtual:Buffer.from(virtual);if(!cache.has(key))cache.set(key,read(p));const b=cache.get(key);return encoding?b.toString(typeof encoding==='string'?encoding:encoding.encoding):b},readdirSync(p,options){const key=String(p);if(!directories.has(key))directories.set(key,fs.readdirSync(p,options));return directories.get(key)},mkdirSync(){throw Error('No real writes authorized')},writeFileSync(){throw Error('No real writes authorized')}}
const module=new Module(native);module.filename=native;module.paths=Module._nodeModulePaths(root+'/app/scripts');module.require=s=>s==='node:fs'?vfs:require(s);module._compile(ts.transpileModule(read(native,'utf8').replaceAll('import.meta.url',JSON.stringify(pathToFileURL(native).href)),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022,esModuleInterop:true}}).outputText,native);const {buildApplicabilityCompilation}=module.exports
let before,after;const changes=[]
before=buildApplicabilityCompilation();for(const id of ids){const g=c.goals.find(g=>g.id===id),override=g?.extendedData?.applicabilityOverrides;if(!override)throw Error('Missing reviewed exact override '+id);changes.push({goalId:id,removedField:override});delete g.extendedData.applicabilityOverrides}virtual=JSON.stringify(c,null,2)+'\n';after=buildApplicabilityCompilation()
const all=r=>new Map(r.reports.flatMap(x=>x.goals.map(g=>[x.landscapeId+':'+g.goalId,g.compiledApplicability]))),bm=all(before),am=all(after),deltas=[...new Set([...bm.keys(),...am.keys()])].filter(key=>JSON.stringify(bm.get(key))!==JSON.stringify(am.get(key))).map(key=>({key,before:bm.get(key),after:am.get(key)})),own=r=>r.reports.find(r=>r.landscapeId===c.landscapeId),bw=own(before).findings.filter(f=>f.severity==='warning'),aw=own(after).findings.filter(f=>f.severity==='warning'),key=f=>[f.code,f.goalId,f.dimension,f.value].join('|'),removed=bw.filter(b=>!aw.some(a=>key(a)===key(b))),added=aw.filter(a=>!bw.some(b=>key(a)===key(b))),stableCanonical=read(file,'utf8')===bytes
const status=deltas.length===0&&removed.length===7&&removed.every(f=>f.code==='APV-201'&&ids.includes(f.goalId))&&!added.length&&stableCanonical?'PASS':'FAIL'
console.log(JSON.stringify({schemaVersion:1,status,mode:'read-only-seven-override-removal-shadow-cached-input-filesystem',checkedAt:new Date().toISOString(),humanApproval:false,operativeWrites:0,baselineChanges:0,beforeCanonicalSha256:hash(bytes),realCanonicalUnchanged:stableCanonical,snapshotInputFiles:cache.size,allCanonicalGoalApplicabilitySetsCompared:bm.size,allCanonicalApplicabilityDeltas:deltas,removedWarnings:removed,addedWarnings:added,changes,ownBeforeSummary:own(before).summary,ownAfterSummary:own(after).summary,meaning:status==='PASS'?'Exact redundant additive overrides can be removed without changing any compiled applicability value; separate APV-203 stale committed fields remain unresolved.':'FAIL: no invariance or removal conclusion is authorized.'},null,2))
if(status!=='PASS')process.exitCode=1
