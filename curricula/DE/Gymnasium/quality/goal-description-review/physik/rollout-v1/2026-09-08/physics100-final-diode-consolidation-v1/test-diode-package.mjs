// Read-only exact-current test. Never regenerates files or weakens native validators.
import fs from 'node:fs'
import path from 'node:path'
import {createHash} from 'node:crypto'
import {createRequire} from 'node:module'
import {execFileSync} from 'node:child_process'
import {pathToFileURL} from 'node:url'
import {base,paths,ids,children,tasks,landscapeId} from './authoring-spec.mjs'
const root=process.cwd(),read=p=>JSON.parse(fs.readFileSync(p)),mod=p=>import(pathToFileURL(root+'/'+p)),hash=x=>'sha256:'+createHash('sha256').update(x).digest('hex'),assert=(v,m)=>{if(!v)throw Error(m)}
const helper=await mod('app/scripts/lib/physicsFinalDiodeViewPlacements.ts'),comp=await mod('app/src/utils/authoring/compositionViewAuthoring.ts'),canon=await mod('app/src/utils/authoring/canonicalAuthoring.ts'),rt=await mod('app/src/utils/compositionViewRuntime.ts'),{normalizeLandscape}=await mod('app/src/hooks/useLandscapes.ts'),{fingerprintSemanticKindSourceGoal}=await mod('app/scripts/goalBookModel.ts')
const c=read(paths.canonical),k=read(paths.kinds),by=new Map(c.goals.map(g=>[g.id,g])),kind=new Map(k.decisions.map(d=>[d.goalId,d])),math=read('curricula/DE/Gymnasium/canonical/DE_DEU_S_GYM_CANONICAL_MATHEMATIK.de.json'),decorated={...c,goals:c.goals.map(g=>({...g,semanticKind:kind.get(g.id)?.semanticKind}))},nc=canon.normalizeCanonicalLandscape(decorated),nm=canon.normalizeCanonicalLandscape(math),u=canon.normalizeCanonicalLandscape({...decorated,goals:[...math.goals,...decorated.goals]})
const compile=v=>comp.compileCompositionView(comp.normalizeCompositionView(v),nc,u,new Map([[landscapeId,nc],[math.landscapeId,nm]]))
const targets=r=>{const s=new Set();const walk=n=>{if(n.sourceGoalId)s.add(n.sourceGoalId);n.children?.forEach(walk)};r.compiledRootNodes.forEach(walk);return [...s].filter(id=>id!=='bf980fff-b62b-4ea4-a20d-31681a7ad785').sort()} // compare learner targets, not the structural compatibility root
const runtimeEntries=[normalizeLandscape(decorated),normalizeLandscape(math)]
const rawRuntimeReachable=v=>{const e=rt.applyCompositionViewProjection(runtimeEntries,v).find(e=>e.meta.landscapeId===landscapeId),m=new Map(e.goals.map(g=>[g.id,g])),s=new Set();const visit=id=>{if(s.has(id))return;s.add(id);m.get(id)?.contains.forEach(visit)};e.goals.filter(g=>g.tags.includes('root')).forEach(g=>visit(g.id));return [...s].filter(id=>by.has(id)&&id!=='bf980fff-b62b-4ea4-a20d-31681a7ad785').sort()}
// Stored/reachable support nodes are not necessarily learner-scope targets.
// The unchanged runtime scope API, not raw contains reachability, is authoritative.
const runtime=v=>rawRuntimeReachable(v).filter(id=>rt.compositionViewExposesGoal(runtimeEntries,v,id))
const receipt=read(base+'/authoring-receipt.json'),stage=read(base+'/stage-delta.json'),currentViews=[]
for(const row of stage.rows){const v=read(row.path),r=compile(v),t=targets(r),p=runtime(v),raw=rawRuntimeReachable(v),selected=helper.expectedDiodeTargets(v.scope).map(i=>children[i]);assert(JSON.stringify(children.filter(id=>t.includes(id)))===JSON.stringify(selected),'Child target matrix '+row.path);assert(JSON.stringify(children.filter(id=>p.includes(id)))===JSON.stringify(selected),'Runtime child matrix '+row.path);assert(!t.includes(ids.parent)&&!p.includes(ids.parent),'Old parent became target '+row.path);const copy=helper.preserveFinalDiodePlacementsFromCurrent(structuredClone(v),v);assert(JSON.stringify(copy)===JSON.stringify(v),'Identity preservation '+row.path);currentViews.push({path:row.path,sha256:hash(fs.readFileSync(row.path)),targetChildren:selected,compileErrors:r.findings.filter(f=>f.severity==='error'),runtimeChildTargets:children.filter(id=>p.includes(id)),targetsSha256:hash(JSON.stringify(t)),runtimeTargetsSha256:hash(JSON.stringify(p)),allCompileRuntimeTargetsEqual:JSON.stringify(t)===JSON.stringify(p),rawReachableNonTargetIds:raw.filter(id=>!p.includes(id)),runtimeOnlyTargets:p.filter(id=>!t.includes(id)),compilerOnlyTargets:t.filter(id=>!p.includes(id))})}
const fixture=read('curricula/DE/Gymnasium/composition-views/physik/de-he-gk.view.json'),negative=[]
for(const [name,change] of [['jurisdiction',v=>v.scope.jurisdiction='DE-RP'],['stage',v=>v.scope.stage='SekI'],['duration',v=>v.scope.durationModel='G8'],['missing stage anchor',v=>{const prune=ns=>ns.filter(n=>!/^Sekundarstufe II/.test(n.label??'')).map(n=>n.children?{...n,children:prune(n.children)}:n);v.rootNodes=prune(v.rootNodes)}]]){let error=null;const v=structuredClone(fixture);change(v);try{helper.preserveFinalDiodePlacementsFromCurrent(v,fixture)}catch(e){error=e.message}assert(error,'Guard must reject '+name);negative.push({name,error,status:'PASS'})}
for(const [name,change] of [['missing role guard',v=>{const prune=ns=>ns.filter(n=>n.goalId!==ids.parent).map(n=>n.children?{...n,children:prune(n.children)}:n);v.rootNodes=prune(v.rootNodes)}],['illegal parent promotion',v=>{const w=ns=>ns.forEach(n=>{if(n.goalId===ids.parent)n.projectionRole='target';w(n.children??[])});w(v.rootNodes)}]]){const v=structuredClone(fixture);change(v);let error=null;try{helper.preserveFinalDiodePlacementsFromCurrent(structuredClone(v),v)}catch(e){error=e.message}assert(error,'Role guard must reject '+name);negative.push({name,error,status:'PASS'})}
const originalImageLinks=receipt.parentBefore.resourceLinks;assert(JSON.stringify(originalImageLinks)===JSON.stringify(by.get(ids.parent).resourceLinks),'Parent image changed');assert(JSON.stringify(by.get(ids.switch).requires)===JSON.stringify([ids.model]),'Switch overrequires measurement/rectifier');assert(by.get(ids.parent).contains.length===3&&by.get(ids.parent).requires.length===0,'Parent aggregation');for(const id of [ids.parent,ids.switch,...children,...tasks.map(t=>t.id)])assert(kind.get(id).sourceFingerprint===fingerprintSemanticKindSourceGoal(by.get(id)),'K stale '+id)
for(const field of ['contains','requires']){const done=new Set(),active=new Set();const visit=id=>{if(done.has(id))return;assert(!active.has(id),field+' cycle '+id);active.add(id);for(const other of by.get(id)?.[field]??[])visit(other);active.delete(id);done.add(id)};c.goals.forEach(g=>visit(g.id))}
const assessmentChecks=tasks.map(t=>{const g=by.get(t.id),e=g.examData;assert(JSON.stringify(g.requires)===JSON.stringify(e.coveredGoalIds)&&JSON.stringify(g.requires)===JSON.stringify([children[tasks.indexOf(t)]]),'Exact task coverage '+t.id);for(const key of ['taskContent','taskContentEn','solutionContent','solutionContentEn','scoring'])assert(JSON.stringify(e[key])===JSON.stringify(t.examData[key]),'Body differs from authored spec '+t.id);assert(e.scoring.steps.reduce((n,s)=>n+s.points,0)===e.scoring.maxPoints&&e.scoring.passingPoints>e.scoring.maxPoints-8,'Essential unit bypass');return {goalId:g.id,reviewStatus:e.reviewStatus,coveredGoalIds:e.coveredGoalIds,bodySha256:hash(JSON.stringify({taskContent:e.taskContent,taskContentEn:e.taskContentEn,solutionContent:e.solutionContent,solutionContentEn:e.solutionContentEn,scoring:e.scoring})),essentialGateCannotBeBypassed:true}})
const released=read(base+'/assessment-release-receipt.json');for(const a of assessmentChecks){assert(a.reviewStatus==='released','Task release '+a.goalId);assert(released.records.find(r=>r.goalId===a.goalId)?.bodySha256===a.bodySha256,'Actual counterreview body hash '+a.goalId)}
for(const parent of ['21ab0854-4d67-5233-9495-ae208e152a3c','85bbad98-2f48-5d64-85c4-ab6cf67f24c2']){assert(by.has(parent),'Local autonomy parent missing '+parent);for(const task of tasks)assert(by.get(parent).contains.includes(task.id),'Existing released task missing from local autonomy '+parent)}
for(const panel of read(base+'/panel-reuse-receipt.json').rows){assert(hash(fs.readFileSync(panel.sourceSvg))===panel.sourceSvgSha256,'Parent SVG changed');for(const key of ['draftPng','canonicalPng','publicPng','backendPng'])assert(hash(fs.readFileSync(panel[key]))===panel.pngSha256,'Counterreviewed panel bytes '+key+' '+panel.goalId);const svg=fs.readFileSync(panel.canonicalPng.replace(/\.png$/,'.svg'),'utf8'),original=fs.readFileSync(panel.sourceSvg,'utf8');assert(svg.slice(svg.indexOf('>')+1)===original.slice(original.indexOf('>')+1),'Panel inner SVG changed '+panel.goalId)}
const bindings=read(base+'/source-bindings.json').bindings;for(const b of bindings){const m=read(b.mappingPath),d=m.decisions.find(d=>d.sourceGoalId===b.sourceGoal.id);for(const id of b.canonicalGoalIds)assert(d.canonicalGoalIds.includes(id)&&m.mappings.some(x=>x.legacyGoalId===d.sourceGoalId&&x.canonicalGoalId===id&&x.matchType==='partial'),'Source binding '+d.sourceGoalId);assert(!d.canonicalGoalIds.includes(ids.parent),'Broad source parent retained '+d.sourceGoalId)}
const allCards=fs.readFileSync('curricula/DE/Gymnasium/quality/memory-card-review/canonical-physics-full.cards.review.jsonl','utf8').trim().split('\n').map(JSON.parse);assert(!allCards.some(r=>(r.originGoalIds??[]).some(id=>[ids.parent,...children].includes(id))),'Unexpected card ownership')
const require=createRequire(root+'/app/package.json'),ts=require('typescript'),generatorTests=[]
for(const state of ['He','By','Bw','Rp','Sl','Sn','Th','St','Bb','Hh']){const sourcePath=root+'/app/scripts/generate'+state+'PhysicsSourceExtraction.ts',writes=new Map(),cache=new Map(),logs=[]
 const vfs={...fs,readFileSync(p,encoding){const key=path.resolve(String(p));if(!writes.has(key))return fs.readFileSync(p,encoding);return encoding?writes.get(key).toString(typeof encoding==='string'?encoding:encoding.encoding):writes.get(key)},existsSync:p=>writes.has(path.resolve(String(p)))||fs.existsSync(p),mkdirSync(){},writeFileSync(p,data){writes.set(path.resolve(String(p)),Buffer.isBuffer(data)?data:Buffer.from(data))},rmSync(){throw Error('No destructive generator action')},renameSync(){throw Error('No rename generator action')}}
 function load(file){if(cache.has(file))return cache.get(file).exports;if(file.endsWith('.json'))return read(file);const m={exports:{}};cache.set(file,m);const src=fs.readFileSync(file,'utf8').replaceAll('import.meta.url',JSON.stringify(pathToFileURL(file).href)),js=ts.transpileModule(src,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022,esModuleInterop:true}}).outputText;const req=s=>{if(s==='node:fs'||s==='fs')return vfs;if(s==='node:child_process')return {execFileSync(cmd,args,opts){assert(cmd==='pdftotext'&&args.at(-1)==='-','Read-only subprocess');return execFileSync(cmd,args,opts)}};if(s.startsWith('.')){let f=path.resolve(path.dirname(file),s);if(!path.extname(f))f+='.ts';return load(f)}return createRequire(file)(s)};new Function('require','module','exports','console',js)(req,m,m.exports,{log:(...a)=>logs.push(a.join(' ')),error:(...a)=>logs.push(a.join(' '))});return m.exports}
 let error=null;const originalCwd=process.cwd();try{process.chdir(root+'/app');load(sourcePath)}catch(e){error=e.stack}finally{process.chdir(originalCwd)}
 const mappingChecks=[],viewChecks=[]
 for(const [file,bytes] of writes){if(file.endsWith('.review.json')&&file.includes('/mapping/')){const m=JSON.parse(bytes.toString());for(const b of bindings.filter(b=>path.resolve(b.mappingPath)===file)){const d=m.decisions.find(d=>d.sourceGoalId===b.sourceGoal.id);mappingChecks.push({sourceGoalId:b.sourceGoal.id,componentIds:b.canonicalGoalIds,pass:!!d&&b.canonicalGoalIds.every(id=>d.canonicalGoalIds.includes(id))&&!d.canonicalGoalIds.includes(ids.parent)})}}if(file.endsWith('.view.json')&&fs.existsSync(file)){const v=JSON.parse(bytes.toString()),current=read(file);const own=v=>{const a=[];const walk=(ns,ancestors=[])=>ns.forEach(n=>{if((ancestors.includes('physics-final-diode-prerequisite-practice')&&n.goalId==='1abfd5ef-1f42-5b71-8c9a-80c0a6b0322e'&&(n.projectionRole??'target')==='target')||helper.diodeOwnedIds.has(n.goalId)||(ancestors.includes('physics-final-diode-seki')&&n.projectionRole==='prerequisiteOnly'&&['df010b2b-b182-5f7e-bbe4-49b72e48c27a','7badac4d-2874-5b3a-87e8-bf8f4440b2a6'].includes(n.goalId)))a.push({node:n,ancestors});walk(n.children??[],[...ancestors,n.id])});walk(v.rootNodes);return a};viewChecks.push({path:path.relative(root,file),pass:JSON.stringify(own(v))===JSON.stringify(own(current))})}}
 generatorTests.push({state,status:!error&&mappingChecks.every(x=>x.pass)&&viewChecks.every(x=>x.pass)?'PASS':'FAIL',error,capturedWrites:writes.size,realFilesystemWrites:0,mappingChecks,viewChecks,sourceSha256:hash(fs.readFileSync(sourcePath))})
}
const nativeErrors=currentViews.flatMap(r=>r.compileErrors.map(e=>({path:r.path,...e}))),status=generatorTests.every(g=>g.status==='PASS')&&nativeErrors.length===0&&currentViews.every(v=>v.allCompileRuntimeTargetsEqual)?'PASS':'FAIL'
console.log(JSON.stringify({schemaVersion:1,checkedAt:new Date().toISOString(),status,scope:'Current exact diode graph, source components, stage/role guards, native compiler/runtime projection and actual source generator execution in an in-memory output filesystem. Global native errors remain errors.',humanApproval:false,realFilesystemWrites:0,nativeErrors,negativeGuards:negative,assessmentChecks,currentViews,generatorTests,cardOwnership:{parentOrChildrenReferenced:false,existingPrerequisiteCard:'physics_q4_c16; unchanged band-model recall, not a diode competency substitute'}},null,2));if(status!=='PASS')process.exitCode=1
