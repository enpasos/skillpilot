# B034 informed completion — operative core applied

Status on 2026-09-08: the bounded canonical/source/view/A/M/K change set is applied. Seven new ordinary atoms (the six independently assessable children plus a narrow thermal-reactor function), four stable historical IDs retained as clusters, the refined existing amplifier, seven new concrete assessments and repaired switch assessment 88f are present. All eight tasks are locally `released` after the separately assigned informed AI counter-review kept all eight exact bodies, solutions and scoring contracts. No human approval, blind D review or deployment is claimed. The original assessment specs deliberately preserve their reviewed draft provenance; the guarded `release-assessments.mjs` and `assessment-release-receipt-a-20260908.json` document the operative status-only transition, individual A findings and independent B body hashes.

The original pre-implementation audit is preserved in `informed-consolidation-audit-a-20260908.md`. `authoring-spec.mjs`, the three assessment specs and `materials/` contain the exact bilingual competence and task/material/solution bodies. Audio and switch S-mode require a genuinely observed personal build. Their indivisible 8-point evidence unit cannot be compensated by the other 8 points below the 10-point pass threshold; D certifies no personal build.

The historical September 6 emitter/lease was not replayed or silently refreshed. The current `field-leased-plan.json` was captured from the current fields. Native CPV-004/009 failures were corrected by explicitly expanding only affected paths and retaining old clusters as prerequisite-only subtree references; CPV-007 empty presentation structures were pruned without refreshing before-leases. `failed-native-view-plan-1.json` records the rejected pre-apply candidate. The final native candidate check passed 2,028 leased operations across 87 files and all 70 Physics views including the atlas. Operative application used minimal native diffs through apply_patch; concurrent unrelated canonical, generator and view fields were preserved.

Seven Nano Banana Pro images were generated through the explicitly requested local agent route with `--no-import`, individually inspected in the original, and imported only after acceptance. Four first candidates were rejected for concrete factual/diagram defects. `individual-image-review.json` records every inspected candidate and rationale. Native Physics QA binds the seven AI decisions to actual image hashes; human flags remain no. The existing amplifier image was inspected and retained. Prompt and image-reconstruction files accompany imported assets.

Eight individual positive-evidence profiles are native-validated current AI candidates at `positive-evidence.config.json`, `positive-evidence.candidates.json` and `positive-evidence.review.jsonl`. They are unregistered, `needs_human_review` / `ai_candidate`, E1/G1, with no invented review-run IDs. D and recognition/registry work remain root-owned.

Post-apply native checks passed: A 469/469 current atomic decisions; M 469 current decisions with no open/stale/obsolete/card-visibility issue; all 732 semantic-kind fingerprints current; 297 repository composition views valid; 1,544 visualization links across 21 landscapes asset-valid. These counts describe the checked snapshot, not later parallel additions.

The final task-visibility follow-up is applied in 32 views: 24 non-HE LK views receive the concrete amplifier endpoint only where af50 was already a target, and 8 HE/RP views move the switch task out of the inherited Sek-I exercise folder into the local material-based task group. `assessment-view-followup-receipt-a-20260908.json` records native validation, exact before/after hashes, added task targets and zero removed old targets. Neither this follow-up nor the release changed any task body, unrelated canonical goal or unrelated K decision. The 32 material files remained exact.

Subsequent native route diagnostics required explicit stage corrections, recorded in the stage-placement (39 views), nested-guard/reactor-link (4 views and the approved existing SekI cluster link), terminal-scope (9 views), and BW-energy-support (2 views) receipts. These preserve ordinary goal semantics. The BW endpoint uses the existing qualitative mass-energy task; national switch support is prerequisite-only. Current native stage selection agrees between GK and LK in all 16 jurisdictions. No broad old nuclear/transistor cluster is used to create an unintended SekI target obligation.

The CQR003 follow-up distinguishes two different defects. Exactly 76 redundant f74/c648 references in 56 views were synchronized to their already-effective prerequisite-only role: the entire native compiled target set stayed identical, and actual runtime membership lost only those previously excluded IDs. The mixed candidate was correctly rejected because af50 was a real effective target, not a runtime-only duplicate. After a separate explicit scope decision, exactly 24 unsupported state LK views lost af50 and only its concrete amplifier task 144007; af50 remains prerequisite-only. HE LK2, national LK2 and atlas remain unchanged. The earlier 24 added amplifier task occurrences are thus deliberately superseded, not claimed as current source-backed targets. `amplifier-scope-receipt-a-20260908.json` binds the exact two-goal target/runtime delta for every affected view.

Direct current amplifier mapping and provenance identify only HE LK Q4.5, p.46, bullet 10. BW/BY/RP already excluded the amplifier as a target; their absence from the unsupported-source list is not evidence of an amplifier requirement. `amplifier-source-scope-audit-a-20260908.md` records that distinction and the exact task placements.

`physicsB034ViewPlacements.ts` preserves only explicit reviewed references at their exact current stage paths, never a whole view. It is installed at the BB/BE, HH, RP, SN, SL and TH generator seams; existing Astro protection remains. Tests execute actual source generators using an in-memory output filesystem, so no source extraction, registry, mapping or operative view is regenerated. The first test's TH failure was a harness expectation error: TH already deliberately skips existing authored views; the corrected receipt distinguishes that no-write path from actual helper replay. Unrelated HH/RP template target deltas are recorded, not silently fixed. The helper also rejects a changed jurisdiction or a missing foreign stage anchor and passes isolated strict TypeScript and ESLint.

The approved punctuation correction changes only the 49872 cluster description, its own K fingerprint and the same authoring expression. Its QA synchronization changes only that record's descriptive metadata, not image bytes or any approval; all other QA rows remain exact. Native A and M checks on the current 473-ordinary-goal snapshot pass, with 146 kept primary memory cards. The supplementary direct-route helper still reports the exact pre-existing HEAD-identical cross-landscape acceleration→Mathematics-cluster compatibility edge documented in `preexisting-cross-landscape-compatibility-receipt.json`; it is not suppressed or converted into invented prerequisites. Full current native CQR104/source-audit and global M6/maturity-floor publication remain root-owned.

NTG, GA-ASTRO and optional-electronics choices are honest source-profile annotations over jurisdiction/stage/course union views, not implemented runtime selection. No nationwide source-completeness, Physics-100%, mastery transfer, human approval or D/P completion is asserted.
