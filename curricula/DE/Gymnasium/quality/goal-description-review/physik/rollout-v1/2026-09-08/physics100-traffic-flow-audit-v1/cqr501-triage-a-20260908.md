# CQR-501: bounded read-only triage

No operative canonical, source, view, acceptance-baseline or validator change was made during this diagnosis. New Astro/diode authoring was not substantively reviewed.

## Time and evidence boundaries

The stored quality status reports28 active warnings on the earlier750-goal state:21 APV-203 and7 APV-201. Its CQR-501 entry stores aggregate counts, not the28 individual IDs. The retained `tmp/applicability/7f6fc60c-9fcc-4cc2-b07e-f897a1d0338a.json` is much older (718 goals,1 already accepted warning), so it cannot be substituted as the28-warning evidence.

The fresh native compiler initially saw764 active goals,42 raw warnings including1 already accepted warning:10 APV-201 and31 APV-203 active. That complete, hash-bound current snapshot is `cqr501-current-native-triage-a-20260908.json`; it records exact goal IDs, current/compiled countries, every evidence reference grouped without dropping sources, and HEAD comparisons. Concurrent Astro source mapping integration then resolved4 warnings without any action by this reviewer. Do not report the moving41 or37 active counts as a stable historical28-row list. Findings about newly integrated foreign children are recorded only as native diagnostics, not judgments of those authors' drafts.

## Small safe correction: seven redundant B034 overrides

APV-201 means an explicit additive applicability override exists. It does not mean that the source is missing, nor does an override restrict applicability to its listed countries. All seven B034 overrides duplicate current direct reviewed extraction mappings:

| Goal | Override | Existing direct source components |
|---|---|---|
| 68034218 reactor function | BY | BY1294d659 Ph10.4.3;d5355d5d Ph13-GA.4.8;096063c6 Ph13-EA.4.10 |
| 69280706 stellar end states | BY | BYf2d7d811 Ph13-GA-ASTRO.4.4, initial mass/mass loss component |
| 7d4d6a39 transistor switch | BY,HE,RP | BYa4461a25/70a98347 Ph10 NTG5;HEQ4.5 b04/b07/b08;RP electronics switching/basic-circuit rows, pp.38/68 |
| 922f32ba audio-light experiment | BY | BY6a715839 Ph13-EA.5.2, audio construction component |
| c2af45aa bistable flipflop | HE | HEQ4.5 b08 `Transistor als Schalter (Flip-Flop-Schaltung)` |
| e1b21fe9 heavy-element argument | BY | BYf2d7d811 Ph13-GA-ASTRO.4.4, material-based argument component |
| f505f039 multiplex model | BY | BY6a715839 Ph13-EA.5.2, multiplex model component |

The existing direct edges are partial component mappings; no promotion to exact is needed. The source statements remain available without these override fields.

The successful native cached-input shadow (`cqr501-seven-overrides-cached-shadow-a-20260908.json`) removes exactly these seven fields in memory. It compares all5798 canonical compiled goal-applicability sets across all landscapes:0 differences; exactly7 APV-201 disappear;0 new warnings. The actual canonical file remains byte-identical. Therefore a minimal follow-up can remove only those seven `extendedData.applicabilityOverrides` fields, keep their `applicabilityMappingInheritance: boundary`, and synchronize any affected native semantic bindings. This is data cleanup backed by identical computed meaning, not warning acceptance. It is proposed, not applied.

The first shadow attempt is retained as FAIL/discarded evidence because its filesystem adapter did not replace statically bound compiler reads and concurrent source writes contaminated the comparison. No conclusion is drawn from that attempt. The passing replay uses the unchanged native compiler body with a cached read-only filesystem and actual override substitution.

## APV-203: do not confuse stored cache drift with new source obligations

APV-203 is raised at `app/scripts/applicabilityCompiler.ts:1366` when the committed `applicability` field differs from the current compiled value. The compiler uses direct/inherited mapping, child union and prerequisite closure; it does not equate every compiled country with a source-mandated target. Current composition roles and source coverage remain separate authorities.

Own B034 mismatches:

| Goals | Stored → compiled | Exact cause |
|---|---|---|
| ea17b0af optical aggregate;7df923a0 stellar aggregate |16 countries → BY | Both reviewed boundary clusters now have exactly two BY-supported children; old aggregate fields were retained. Narrowing these two derived fields to BY follows actual child union. |
| 68034218 reactor;74a32716 reactor task | BY →16 countries | Ordinary prerequisite closure reaches the whole49872 nuclear cluster; recursive cluster expansion includes the BY reactor child. The task derives its applicability from that child. |
| c2af45aa flipflop;5902ebe8 task | HE →15 non-BY countries | Whole d367 transistor cluster is still required by broad retained tasks; recursive expansion reaches flipflop. The real task inherits the resulting countries. |
| 7d4d6a39 switch | BY,HE,RP →16 countries | Closure through d367 plus af50 and c2; boundary stops inherited source mapping but deliberately does not stop prerequisite closure. |
| 144007cb amplifier task | HE →15 non-BY countries | `assessment-requires` from af50; af50 is still a direct prerequisite of broad0f5346 and4a58 tasks as well as a child of d367. |

Adjacent nuclear mismatches (`3df08a2c`, `50877233`, `7d78da7f`, `a77138b8`, `ca13e9cd`, `85bbad98`) come from the same nuclear prerequisite expansion, genuine ordinary energy prerequisites, assessed-goal-derived task applicability, or child union. Their full sources/deltas are retained in the native JSON. These are not new source claims created by 1232/4a2; neither traffic goal is named in the warning snapshot.

The precise broad task relationships are:

- 5a5bc118 and4a58 require the whole49872 cluster.
- 0f5346 and4a58 require the whole d367 cluster and directly require af50.
- `ensureRequiredGoalVisible` recursively expands a required cluster's children, even when the cluster itself has `requires: []`. Removing cluster-level requires would therefore solve nothing:49872 and d367 already have none.

Two distinct follow-up choices must not be conflated. If these broad prerequisites are genuinely intended for retained all-topic assessment contexts, the committed applicability cache can be synchronized to compiled support eligibility, but all explicit learner target sets and exact source coverage must stay unchanged. If the broad tasks no longer genuinely assess each new cluster child, their actual bodies/criteria must be reviewed and their specific `requires` plus `coveredGoalIds` narrowed to what is assessed, preserving real local endpoints. Do not infer a blanket need for reactor, amplifier or flipflop targets in every country, add surrogate source claims, or blindly delete prerequisite edges to quiet the compiler.

Recommended order: remove the7 proven redundant overrides; synchronize the2 unambiguous aggregate BY child-union fields; then explicitly choose/verify the intended support semantics of the retained broad assessment references before materializing remaining APV-203 fields. Keep the normal CQR-501 and protected M6 gates unchanged throughout. This diagnosis grants no permission to perform those follow-up writes.
