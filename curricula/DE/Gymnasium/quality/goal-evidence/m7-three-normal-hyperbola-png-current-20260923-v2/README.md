# Three Math P-v2 candidates after the a777 integral-prose correction

This is a new, separate AI-candidate rebind for exactly `3f1a97d2`, `8c32d941`, and `a7778885`. It leaves the v1 three-goal P package and its six-goal retained split unchanged. The previous v1 review is historical for `a7778885` after the canonical German/English text edit; an unchanged profile object does not make its old goal/input fingerprint current. No human approval, learner evidence, or M7 completion is claimed.

## Profile × current text × original image

| Goal | Current competence and image | Independent P-v2 case check |
| --- | --- | --- |
| `3f1a97d2` | The J8 DE/EN graph-to-term wording is unchanged. The original PNG (`97cf6db334cb1c14c818d0f18cd79bf28f1b412bb4eec9d958336053631fbb26`) works through `f(x)=2/(x−1)+3`, its asymptotes, and three consistent points. | The unchanged P cases reconstruct `6/(x−2)−1` and `−2/(x+2)+3` from independently supplied graph features, with different asymptotes, point checks, and a negative-branch transfer. The image cannot supply their answers. |
| `8c32d941` | The Q3 DE/EN probability-and-context wording is unchanged. The original PNG (`09c914227f618c81397d1c6e65b2844ad90e75c9087b5828fbaadc36f7f2b91a`) marks only a generic normal interval `[a,b]`. | The unchanged P cases require a numerical central probability with contextual meaning and a separate upper-tail event in a different distribution. Neither parameters nor answers are in the image. |
| `a7778885` | The corrected DE/EN description states in readable prose that the normal CDF value at `x` is the integral of its density from `−∞` to `x`, hence the accumulated left probability. This preserves the prior integral relation and the density/area/value distinction. The original PNG (`0aab1d68fb4f8e45fa13525b19a836fb00a2efa921822f397ac7f32c7144ef94`) shows only generic left accumulation `F(x)=P(X≤x)`. | The unchanged P profile explicitly distinguishes accumulated area from density height, explains change with the boundary and interval differences, and transfers from the standard normal to a shifted/spread-out one. Its numerical `Φ` values, interval difference, and local-change explanation are not supplied by the image. The prose correction neither removes nor adds a competence requiring a case rewrite. |

The three complete profile objects in `positive-evidence.candidates.json` are byte-for-byte equivalent as JSON to those in the pinned v1 review; only candidate metadata and explanatory reasons are new. All three remain `E1/G1` AI candidates. `source-pins.json` records exact v1 config/candidate/review hashes, the six-record rest config/review hashes, all transferred raw-line and profile fingerprints, current a777 DE/EN wording, and the three image hashes. The six-record rest stays at the v1 package path and is not copied or re-reviewed here.

A read-only check of the historical v1 three-goal config now reports exactly two blockers, both on `a7778885`: stale `goalFingerprint` (current expected `f2f2dc1f7949662ad645b07d98f456386e2ee888e29dd1d81f662755ed691a9d`) and stale `reviewInputFingerprint` (current expected `cc38c73c4a76b95c817bdaf2234823993e4d473e3a1d531d62af4508dcd842f9`). This is the anticipated metadata invalidation from the DE/EN text correction, not a profile-content rejection; the v1 files are not rewritten.

## Current materialization and checks

The corrected-text GoalBook v2 prepare succeeded with model digest `sha256:e8b4f2d5358f9f34b008aa85184a0d3d77067aefe2e554c682d04ae17e844a6d` and bundle fingerprint `sha256:2f582f1dfcfb908ef29c7fa4f937b4da32e322d6c07651a5bafed7967dfb3689`. Its a777 page has the exact new DE prose and original PNG digest; the other two image digests also match this package. The candidate materializer then wrote `positive-evidence.review.jsonl`, binding all three goals to current semantics and exact original image bytes. The a777 `goalFingerprint` is now `sha256:f2f2dc1f7949662ad645b07d98f456386e2ee888e29dd1d81f662755ed691a9d` and its `reviewInputFingerprint` is `sha256:cc38c73c4a76b95c817bdaf2234823993e4d473e3a1d531d62af4508dcd842f9`; the other two input fingerprints and all three profile fingerprints remain the same as pinned v1.

The v2 positive-evidence checker, no-write candidate reproducibility check, and unchanged six-record rest checker all exited 0: respectively 3, 3, and 6 current `needs_human_review` AI records with no blocking issue. A separate SHA/content audit verified all five pinned v1/rest source files unchanged, exact equality of the three profile objects, current a777 DE/EN text, and all three canonical PNGs. The new config binds `goal-visualization` for all three. Every new review record is `needs_human_review` / `ai_candidate`, `E1/G1`, with no review-run ID or human approval. The central registry, canonical, QA, and GoalBook were not edited by this package; the new P records do not become a registry-bound book profile merely by existing here.
