# Mathematik M7 – P-v2-Erstdurchgang für nächste 20 Oberstufenziele

Der Batch `m7-upper-sec-next20-normal-spatial-methods-current-20260923-v1` umfasst 20 aktuelle disjunkte `curricularAtomic`-Ziele. Jedes Ziel hat ein eigenes fachliches `positive-understanding-evidence-v2`-Profil mit zwei notwendigen Verständniserwartungen, zwei unabhängigen Fallbriefs und einer strukturellen Transferachse. Die Profile sind **KI-Kandidaten** (`needs_human_review`, `ai_candidate`, `E1/G1`), nicht menschlich freigegeben. Die native P-Prüfung wird mit `requireApproved: false` nur als Kandidatenprüfung verwendet. Weder zentrale Registry noch In-flight-Ledger oder Kanonik wurden für diese P-Arbeit geändert.

- `image-bound-nine.config.json` / `.candidates.json` / `.review.jsonl`: neun Profile mit `reviewedResourceTypes: ["goal-visualization"]`, nur für die neun aktuell am Originalpixel fachlich geprüften KEEP-Bilder und deren SHA-256-gebundene QA. Bildbelege und Einzelentscheidungen stehen in `../../goal-visualization-review/math-m7-upper-sec-next20-current-images-20260923-v1/README.md`.
- `text-only-eleven.config.json` / `.candidates.json` / `.review.jsonl`: elf Profile ohne Bildbindung. Neun Ziele haben aktuell kein Bild; `b431148b` und `f2a12269` besitzen SHA-spezifische V-HOLDs. Textgebundenes P ersetzt keinen späteren V-Abschluss.

Ziele und Teilung: bildgebunden `fd13605e`, `3016ec37`, `0a846521`, `fac75b4a`, `58f613da`, `8b885220`, `d0475ed5`, `6fcd6a1a`, `4b16fce6`; textgebunden `8c32d941`, `c406d5a0`, `55d0474b`, `a7778885`, `b431148b`, `3256476b`, `f2a12269`, `a506fc1d`, `5619ca5b`, `fdce0ced`, `79444ef9`.

Native Prüfung am 23.09.2026: beide Materializer-Verify-Läufe und beide `positiveGoalEvidenceReview --mode=check` erfolgreich; 9+11 Kandidaten, jeweils null Blocking Issues, null `approved`. `check:goal-visualization-qa` erfolgreich. Eine zentrale Freigabe oder fachlicher Gesamtabschluss wird damit nicht behauptet.
