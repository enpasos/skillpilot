# Q2 bodies and volume: nine KEEP/KEEP P-v2 candidates

Source: the dual description summary in `goal-description-review/mathematik/rollout-v1/2026-09-23/m7-q2-bodies-volume-14-current-20260923-v1/dual-summary.json`. All nine received `keep` in both rounds, but differing understanding evidence still requires synthesis; no description approval is inferred. The mathematics central P registry had 100 configured reviews and zero matches for these nine goal IDs when scanned on 2026-09-23. No existing P record is replaced.

The six text-only profiles deliberately set `reviewedResourceTypes` to `[]`. They rely on current goal text and mathematics criteria, without an image digest or image-content claim. Pixel inspection supports **V HOLD** for their current visualizations pending correction and separate re-review. This is a finding in this bounded P bundle; it does not change the formal image-QA registry:

| Goal ID prefix | Pixel finding |
| --- | --- |
| `1e77bb2f` | Prism graphic prominently titles the task as a *Formelsammlung* calculation and shows a completed formula-sheet example, while the current competence calls for geometric explanation and use of base area and perpendicular height. |
| `288633c1` | Pyramid graphic titles the task *Formelsammlung* and supplies the full formula and answer; the visual does not demonstrate the claimed one-third relation to a corresponding prism. |
| `2f2c9f1a` | Sphere graphic foregrounds *Formelsammlung* and a worked formula substitution, while the current competence includes explaining cubic radius dependence. |
| `944dd479` | Scalar-triple-product graphic labels the red horizontal-right vector `a=(2,0,0)` while its positive x-axis arrow points down-left. The coordinate depiction contradicts its own component label, even though the printed scalar arithmetic is correct. |
| `c71ae268` | Cylinder graphic titles and describes formula-sheet calculation, including stale goal wording, rather than the current goal's interpretation of the circular base area. |
| `e8237315` | Cone graphic titles the task *Formelsammlung* and supplies the full substitution and answer; it does not show why its volume is one third of the corresponding cylinder. |

The other three current images were inspected at visible pixel level and their relevant content is mathematically coherent: `e9181209` locates the cone's y-coordinate from apex to base consistently with `r(y)=Ry/h` and the disk integral; `eb070ed2` labels consistent parallel and perpendicular relations on a rectangular solid; `636b3e2d` shows cube faces, edges, vertices, and right angles consistently. Their profiles include current image digests, but image binding is not human approval or a V-gate decision.

All nine materialized profiles are `needs_human_review` / `ai_candidate`, with E1 evidence and G1 maximum claim. Each contains two independent, structurally changed cases. This bundle changes no canonical goal, image, D registry, QA decision, or description ledger.
