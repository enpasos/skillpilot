# Retained function evidence after a quality hold

The historical `text-only-three-after-c19-image-20260923-v1` config and
review remain unchanged (review-file SHA-256:
`e990c4c03d588005f1a15197079f19c4a3e629f657164a65faea60bf63fee817`).
The new two-goal file copies the original JSONL lines for `308f19e2…` and
`7156558c…` byte-for-byte. Their source text, page context and P-v2
fingerprints remain current; the scoped production check reports two
`needs_human_review` AI candidates and zero blockers.

The excluded `1b70498a…` record is stale because its image was withdrawn for
a documented mismatch between the falling-ball context and the drawn graph.
This split only preserves unaffected machine evidence. It neither repairs the
held visual nor supplies human approval; a corrected image requires targeted
visual and P-v2 review before this goal can complete M7.
