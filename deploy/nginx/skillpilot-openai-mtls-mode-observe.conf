# Install this root-owned file as /etc/skillpilot/openai-mtls/mode.conf to
# observe real OpenAI connector certificates without blocking certificate-less
# production diagnostics. Never derive this value from request data.
set $skillpilot_openai_mtls_mode observe;
