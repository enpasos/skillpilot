# Install this root-owned file as /etc/skillpilot/openai-mtls/mode.conf only
# after observe-mode evidence confirms the OpenAI connector certificate. In
# this mode certificate-less access is limited to the numeric-loopback operator
# lane classified by the verifier. Never derive this value from request data.
set $skillpilot_openai_mtls_mode enforce;
