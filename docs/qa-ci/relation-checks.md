# Requires Relation Checks

This page is kept as a compatibility entry point.

The maintained rule catalog now lives in:

- `docs/qa-ci/graph-validation-rules.md`

Use that file as the single source of truth for:

- implemented validator rules
- CI enforcement profile
- legacy-warn rollout (`VALIDATE_GRAPH_STRICT_RULES=0`)
