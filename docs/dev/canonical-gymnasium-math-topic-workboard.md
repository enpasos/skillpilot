# Canonical Gymnasium Mathematics Topic Workboard

Snapshot: `2026-04-03`

Purpose:

- steer nationwide canonical math work by **topic first**
- use Bundeslaender as evidence and validation lanes, not as the pedagogical design driver
- make canonical gaps explicit before more state-local rollout work is done

Working rule:

1. pick one high-level topic
2. review the current canonical atom inventory for that topic
3. compare that topic against the reviewed Bundesland sources
4. classify each state cell
5. add canonical atoms only where multiple states or strong pedagogical structure justify them
6. only then deepen or widen state mappings for that topic

Coverage rule:

1. every top-level topic row now runs in `all states required` mode
2. a topic is not done until every Bundesland column is resolved once
3. `seed`, `anchor`, `corr`, `gap`, and `loc` count as resolved cells; only `n/s` means the topic has not yet been checked for that state
4. a frozen canonical package cut is therefore not enough for topic completion; nationwide coverage still has to be closed afterwards

Legend:

- `seed`: canonical topic already materially seeded from the Hessen legacy baseline
- `anchor`: only broad structural or area-level alignment exists
- `corr`: at least one reviewed corridor is already mapped for this topic
- `gap`: current reviewed source evidence suggests a canonical gap or unresolved atomic split
- `n/s`: not started
- `loc`: state-local packaging; do not automatically canonicalize

State columns use Bundesland abbreviations:

- `BW BY BE BB HB HH HE MV NI NW RP SL SN ST SH TH`

## Topic board

| Topic | Canonical | BW | BY | BE | BB | HB | HH | HE | MV | NI | NW | RP | SL | SN | ST | SH | TH | Open canonical gaps | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Sek I Zahlen / Terme / Algebra | `seed` | `n/s` | `n/s` | `n/s` | `n/s` | `anchor` | `anchor` | `seed` | `corr` | `n/s` | `n/s` | `corr` | corr | `corr` | `corr` | `corr` | `corr` | visible `A1-A4` packaging is in place, `SH` confirms that late lower-secondary equation residue does not justify a separate visible `A5`, the Sachsen K10 interest plus K6 shares, K9 function-and-power, K8 terms-and-equations, K8 functions-and-linear-systems, and K7/K6/K5 early-number and fraction corridors now confirm the visible `A1` arithmetic, the broad later `A3` shares/percentage/simple-interest bridge, and the later number-extension surface together with the `A2/A4` boundary while keeping first linear-system residue inside `A4` without forcing a new late-algebra package, and `ST` now adds a first reviewed `JG 5/6` foundations corridor with natural numbers, fractions, rational numbers, and first equations on the frozen `A1/A4` surface, plus reviewed `JG7/8` percentage, variables, and equations/inequalities strips on the visible `A3`/`A2`/`A4` surfaces and reviewed `JG7/8` rational-numbers/roots plus `JG9` powers/logarithms strips on the existing later number-extension surface without forcing separate visible percentage, variable, roots, or logarithm packages beyond the current cut; `RP` now also adds an explicit `Klassenstufen 7 und 8` algebra corridor in which `Rationale Zahlen` stay on the later number-extension surface while `Prozent- und Zinsrechnung` confirms the visible `A3` bridge without forcing a new late-algebra package, and `SL` now also contributes a first reviewed `Klassenstufe 7` algebra corridor in which `Rationale Zahlen` sit on the later number-extension surface, `Prozentrechnung` confirms the visible `A3` bridge, and the mixed `Terme, Gleichungen und Ungleichungen` strip stays deliberately on the broad algebra surface instead of forcing a sharper visible `A2/A4` split or a separate `A5`, plus first reviewed broad `Klassenstufe 8` and `Klassenstufe 9` algebra corridors in which the current `Zahl und Operation` strips stay on the shared Sek-I algebra surface without forcing sharper state-local splits, while `MV` now also adds a first reviewed `Klassen 5/6` arithmetic corridor in which both `Natuerliche Zahlen/Teilbarkeit` and `Brueche/Dezimalbrueche` stay on the visible `A1` arithmetic surface without forcing a new early-algebra package, plus a first reviewed `Klasse 7` algebra corridor in which `Prozent- und Zinsrechnung` confirms the visible `A3` bridge and the broad `ganze/rationale Zahlen + Quadratwurzel` strip stays on the later number-extension surface without forcing a new late-algebra package, and `TH` now also adds a first reviewed `Klassenstufen 5/6` arithmetic corridor in which both `Natuerliche Zahlen` and `Gebrochene Zahlen` stay on the visible `A1` arithmetic surface without forcing a new early-algebra package, plus a first reviewed broad `Klassenstufen 7/8` algebra corridor in which the archived `Arithmetik/Algebra` strip stays on the shared broad Sek-I algebra surface without yet forcing a sharper reviewed split inside the lane, plus a first reviewed broad `Klassenstufen 9/10` algebra corridor in which the archived `Arithmetik/Algebra` strip likewise stays on the shared broad Sek-I algebra surface without yet forcing a sharper reviewed split inside the lane, while the remaining Thueringen packaging pressure is now concentrated more clearly in other `9/10` residual lanes than in algebra | freeze algebra package churn and continue all-state coverage unless a later reviewed lane forces a genuinely new shared late-algebra corridor |
| Sek I Funktionen / Zuordnungen | `seed` | `n/s` | `n/s` | `corr` | `corr` | `corr` | `corr` | `seed` | `corr` | `n/s` | `n/s` | `corr` | corr | `corr` | `corr` | `n/s` | `corr` | visible `F3-F5` packaging is now connected on the first reviewed lanes including the Sachsen `K10` growth/periodicity anchor and the Sachsen K10 late-function strip, and the Sachsen K6 early-correspondence, K8 middle-stage function-and-linear-system, and K9 function-and-power corridors now confirm the early `F1/F2` surface, the current `F3/A4` boundary, the existing `F4` quadratic boundary, and the broad J10 trig/exponential corridor without forcing a new regression package, a new mixed function-equation package, a separate shared power-function package, or a separate shared growth/periodicity package beyond the current surface; ST now adds a reviewed `JG 5/6` correspondence / proportionality corridor that keeps early mappings on the existing `F1/F2` surface and keeps the mixed direct / inverse proportionality residue on the broad proportional bridge without forcing a separate visible inverse-proportional package, a reviewed `JG 7/8` linear-functions strip that sits cleanly on the visible `F3` surface without dragging algebra residue back into the function topic, and a reviewed `JG 9` quadratics strip that sits on the visible `F4` surface without forcing a separate mixed quadratics package beyond the current function/equation boundary, plus a reviewed `JG10` function-classes strip that sits on the broad J10 trig/exponential corridor without forcing a separate mixed function-classes package; `SL` now also contributes a first reviewed lower-secondary `Klassenstufe 7` function corridor with explicit `Zuordnungen`, `proportionale Zuordnungen`, and `umgekehrt proportionale Zuordnungen` strips, keeping early mapping work on the existing early function surface and both proportionality strips on the broad proportional bridge without forcing a visible inverse-proportional package, plus a first reviewed broad `Klassenstufe 8` function corridor in which the current `Strukturen und funktionaler Zusammenhang` strip stays on the shared Sek-I function-foundations surface without forcing a sharper state-local split, and `MV` now also adds a first reviewed `Klasse 7` function corridor with explicit `Zuordnungen` plus a mixed `proportionale / umgekehrt proportionale Zuordnungen` strip that likewise fits the early function surface and the broad proportional bridge without forcing a separate visible inverse-proportional package, a first reviewed `Klasse 8` middle-stage linear-functions corridor in which broad `Funktionen` work sits on the visible function-concept surface while `Lineare und konstante Funktionen` sit cleanly on the visible `F3` surface, a first reviewed `Klasse 9` explicit quadratics corridor in which `Quadratische Funktionen` sit on the visible late Sek-I quadratic-representations surface while `Quadratische Gleichungen` sit on the explicit equation branch, plus a first reviewed `Klasse 10` late function corridor in which both `Exponential-/Logarithmusfunktionen` and `Sinusfunktionen` stay on the broad visible J10 trig/exponential continuation surface without forcing a separate mixed late-function-classes package; `TH` now also adds a first reviewed `Klassenstufen 5/6` functions corridor in which `Zuordnungen` sit on the visible early mappings surface without forcing a broader early function split, plus a first reviewed broad `Klassenstufen 7/8` functions corridor in which the archived `Funktionen` strip stays on the shared broad Sek-I function surface without yet forcing a sharper reviewed split inside the lane, plus a first reviewed broad `Klassenstufen 9/10` functions corridor in which the archived `Funktionen` strip stays on the shared broad Sek-I function surface because it crosses the visible `F4` / late-`J10` boundary too broadly for a narrower reviewed split; the remaining broad HB/HH mixed corridors plus the Sachsen sequence / parametric-curve residue still look more like source-granularity residue than like a new canonical gap | freeze more function package churn unless later lower-secondary lanes force a genuinely new shared atom beyond the current late-J10 continuation surface |
| Sek I Geometrie / Raum | `seed` | `n/s` | `n/s` | `n/s` | `n/s` | `corr` | `corr` | `seed` | `corr` | `n/s` | `n/s` | `corr` | `anchor` | `corr` | `corr` | `n/s` | `corr` | canonical `G2-G7` packaging and the reviewed `HB`/`HH`/`SN` surface are now in place; the Sachsen K5 early-geometry relations, rectangles/cuboids, and K6 prism lanes confirm the existing early geometry/space surface without forcing separate early area-volume or prism packages, the Sachsen K6 triangles/quadrilaterals lane confirms the existing `G2` quadrilateral/triangle/congruence cut, the Sachsen K7 plane-geometry lane confirms the broad later figure/transformation/theorem geometry surface without forcing a separate circle-angle/theorem package, the Sachsen K7 prism/pyramid lane confirms the existing `G6` later solids surface without forcing a separate pyramid package, the Sachsen K8 similarity corridor confirms the existing `G3` transformations/similarity cut, the Sachsen K9 right-triangle corridor confirms the existing `G5` Pythagoras and `G7` trigonometric-bridge cut, the Sachsen K9 circle/circular-solids corridor confirms the existing `G6` circle-and-solids surface, and the broad Sachsen K10 algebraic-geometry corridor stays compatible with the existing later figure/transformation/theorem geometry surface without forcing a separate visible Sek-I analytic-geometry package; ST now adds a first reviewed `JG 5/6` geometry corridor that keeps both geometry basics and early area/volume work on the existing early geometry / space surface instead of forcing a separate early area-volume package, a second reviewed `JG 5/6` corridor that keeps angle relationships together with triangles and quadrilaterals on the existing `G2` triangle / quadrilateral / congruence surface instead of forcing a separate visible early angle package, and a reviewed `JG5/6` quantities strip that sits on the visible early measurement / area / volume surface instead of forcing a separate standalone quantities package; reviewed `JG 7/8` corridors keep circles plus body representation and body calculation on the existing `G6` surface, similarity on the existing `G3` surface, and the Pythagoras strip on the existing `G5` surface instead of forcing any new mixed package; ST now also adds a reviewed `JG9` trigonometry strip that sits on the visible `G7` trigonometric bridge and a reviewed `JG10` vectors strip that sits on the existing Sek-I vector-entry surface without forcing a broader mixed analytic-geometry package, `RP` now also adds an explicit `Klassenstufen 9 und 10` geometry corridor in which geometric transformations stay on the broad later figure/theorem surface, the Pythagoras strip confirms `G5`, body work confirms `G6`, and trigonometric relations confirm `G7`, and `SL` now also adds first reviewed `Klassenstufe 7` geometry and measurement corridors plus first reviewed broad `Klassenstufe 8` geometry and measurement corridors in which the current broad `Raum und Form` strips stay on the shared Sek-I geometry surface and the broad `Groessen und Messen` strips stay on the shared early Sek-I measurement / area / volume surface without forcing sharper state-local splits, while `MV` now also adds a first reviewed `Klassen 5/6` geometry corridor in which broad plane-geometry plus cuboid/cube work stay on the existing early geometry / space surface without forcing a separate visible early area-volume package, a first reviewed `Klasse 7` geometry corridor in which `Dreiecke und Vierecke` sit on the visible `G2` surface and `Kreise` stay on the broad later `G6` circle/body surface without forcing a separate circle-angle package, plus a first reviewed `Klasse 8` geometry corridor in which `Rechtwinkliges Dreieck` confirms the visible `G5` bridge, `Aehnlichkeit` confirms the visible `G3` surface, and the broad `Prismen/Pyramiden/Zylinder` strip stays on the visible `G6` body surface without forcing another mixed geometry package; `TH` now also adds a first reviewed `Klassenstufen 5/6` geometry corridor in which `Figuren und Koerper` together with `Dreiecke und Kreis` stay on the visible early geometry / space surface without forcing a sharper early geometry split, plus a first reviewed broad `Klassenstufen 7/8` geometry corridor in which the archived `Geometrie` strip stays on the shared broad Sek-I geometry surface without yet forcing a sharper reviewed split inside the lane, plus a first reviewed broad `Klassenstufen 9/10` geometry corridor in which the archived `Geometrie` strip likewise stays on the shared broad Sek-I geometry surface without yet forcing a sharper reviewed split inside the lane, while the remaining Thueringen packaging pressure is now concentrated more clearly in other `9/10` residual lanes than in geometry | freeze geometry package churn and continue all-state coverage unless another reviewed lane exposes a concrete shared gap in the canonical cut |
| Sek I Daten / Zufall | `seed` | `n/s` | `n/s` | `n/s` | `n/s` | `anchor` | `anchor` | `seed` | `corr` | `n/s` | `n/s` | `corr` | `anchor` | `corr` | `corr` | `n/s` | `corr` | canonical `D1-D5` package surface is now explicit; the Sachsen K8 chance-experiment strip confirms the existing `D2/D3/D5` surface, the Sachsen K10 discrete-random-variable strip confirms that later Sek-I random-variable work can still be carried by broad `D3/D4`-adjacent partial bridges without forcing a separate visible `D6` package, and the Sachsen K9 descriptive-statistics plus earlier K7 data-display strips confirm the existing `D4` surface while keeping histogram, manipulation, and diagram-reading residue on that same broad package instead of forcing a new visible critical-statistics or diagram-reading package; ST now adds a reviewed `JG 7/8` chance corridor that keeps the broad random-events / frequencies strip on the existing Laplace bridge and keeps the multi-stage-experiment strip on the visible `D3` surface without forcing a new mixed early-probability package, plus a reviewed `JG9` frequency-distributions strip that sits on the visible `D4` descriptive-statistics surface, and a reviewed `JG10` random-variables strip that stays on the broad visible `D1-D5` surface without forcing a separate visible `D6` package, `RP` now also adds an explicit `Klassenstufen 9 und 10` data/chance corridor in which `Vierfeldertafeln, zweistufige Zufallsexperimente` sit on the visible `D3` surface and `Wahrscheinlichkeiten durch Simulationen` sit on the visible `D2` bridge without forcing another package, and `SL` now also adds a first reviewed `Klassenstufe 7` data/chance corridor plus a first reviewed broad `Klassenstufe 8` data/chance corridor in which the current broad `Daten und Zufall` strips stay on the shared Sek-I data/chance surface without forcing sharper state-local splits, while `MV` now also adds a first reviewed `Klassen 5/6` early data/chance corridor in which `Statistische Erhebungen` sit on the visible early data/chance surface and `einstufige Zufallsexperimente` sit on the visible early Laplace bridge without forcing a separate early diagram or early-probability package, a first reviewed `Klasse 9` statistics corridor in which `Haeufigkeiten/Mittelwerte/Streumasse` and `statistische Erhebungen` both sit cleanly on the visible `D4` descriptive-statistics surface without forcing a new survey- or critical-statistics package, and a first reviewed `Klasse 10` later-probability corridor in which `Zufallsexperimente mit und ohne Zuruecklegen` sit on the visible `D3` surface while the broad `Zufallsgroessen/Binomialverteilung` strip stays on the broad visible `D1-D5` surface without forcing a separate visible `D6` package; `TH` now also adds a first reviewed `Klassenstufen 5/6` data/chance corridor in which early `Daten erfassen und darstellen` work stays on the visible early data surface while the mixed `statistische Kenngroessen und mit Zufall experimentieren` strip stays on the broad Sek-I data/chance surface without forcing a separate early diagram or early-probability package, plus a first reviewed broad `Klassenstufen 7/8` data/chance corridor in which the archived `Stochastik` strip stays on the shared broad Sek-I data/chance surface without yet forcing a sharper reviewed split inside the lane, plus a first reviewed broad `Klassenstufen 9/10` data/chance corridor in which the archived `Stochastik` strip likewise stays on the shared broad Sek-I data/chance surface without yet forcing a sharper reviewed split inside the lane, while the remaining Thueringen packaging pressure is now concentrated more clearly in the mixed `9/10` functions strip than in data/chance | freeze package churn here and use additional state lanes only for bridge quality and residue control unless another reviewed lane repeatedly forces a dedicated later random-variable package |
| Sek II Analysis | `seed` | `corr` | `corr` | `corr` | `corr` | `corr` | `corr` | `seed` | `corr` | `corr` | `corr` | `corr` | `corr` | `corr` | `corr` | `corr` | `corr` | visible `AN2-AN4` packaging is now in place, the reviewed `BB`/`BE`/`BW`/`BY`/`HB`/`HH`/`NW`/`SH` realignment pressure passes are connected, the reviewed `NI` late-continuation lane does not justify a separate visible `AN5` package, `SN` now confirms the frozen surface with an explicit GK differential / integral corridor, `ST` now also confirms the frozen surface with a first explicit `GA/EA` analysis split, `SL` now also confirms the frozen surface with a first reviewed upper-secondary analysis corridor in which the broad `G-Kurs` / `Leistungskurs` analysis bands are split into explicit derivative, integral, `e/ln`, and modelling strips plus narrower EP entry anchors without forcing a visible `AN5` package, `MV` now also confirms the frozen surface with a first reviewed broad upper-secondary analysis corridor across limits, derivatives, function investigation, differential applications, antiderivatives, and integral applications, and `TH` now also adds a first reviewed broad upper-secondary analysis corridor in which `Klassenstufe 11: Funktionen` and `Klassenstufen 11/12: Analysis` form a didactically coherent reviewed bridge on the shared analysis surface without forcing a visible `AN5` package; the remaining open cells are now mainly later backlog corridors rather than package blockers | keep package churn frozen and continue all-state closure on the existing `AN2-AN4` surface |
| Sek II Stochastik | `seed` | `n/s` | `corr` | `n/s` | `n/s` | `corr` | `corr` | `seed` | `corr` | `n/s` | `n/s` | `corr` | corr | `corr` | `corr` | `n/s` | `corr` | visible `ST2`, `ST4`, and `ST5` packaging is now in place, `ST3` remains an explicit LK-boundary marker, and the reviewed `BB`/`BY`/`HB`/`HH`/`HE`/`SH` pressure passes do not force a new bridge package; `SN` now also confirms the frozen surface with an explicit GK binomial / inferential strip, and `ST` now adds a first reviewed `GA/EA` stochastics split with conditional probability, binomial distribution, a mixed binomial/normal corridor, and inferential-statistics / confidence-interval residue that still fits the frozen surface, while `SL` now also adds a first reviewed upper-secondary stochastics corridor in which `G-Kurs` / `Leistungskurs` are split into explicit probability, binomial, discrete-random-variable, normal-distribution, and hypothesis-test strips on the frozen `ST2-ST5` surface without forcing another bridge package, and `MV` now also adds a first reviewed broad upper-secondary stochastics corridor in which the current source exposes only one shared `Wahrscheinlichkeitsrechnung und Statistik` band, confirming that the frozen visible surface can absorb a reviewed broad corridor without forcing another bridge package, while `TH` now also adds a first reviewed broad upper-secondary stochastics corridor in which `Klassenstufe 11: Stochastik` sits on the shared `Wahrscheinlichkeiten und Verteilungen` surface and the broad `11/12` stochastics strip stays on the summary `Stochastik, Tests und Statistik` surface without forcing another bridge package, so only broader multi-state residue could reopen this topic | freeze package churn here and reopen only if another reviewed lane shows a real shared canonical gap |
| Sek II Analytische Geometrie / Vektoren | `seed` | `n/s` | `corr` | `n/s` | `n/s` | `corr` | `corr` | `seed` | `corr` | `n/s` | `n/s` | `corr` | corr | `corr` | `corr` | `corr` | `corr` | visible `AGV2-AGV5` packaging is now in place and reviewed `BB`/`BE`/`BY`/`HB`/`HH`/`HE`/`SH` evidence does not force a new shared package; `SN` now also confirms the frozen surface with an explicit GK vector / line-plane / metric strip, `ST` now adds a first reviewed `GA/EA` geometry split with broad `Geraden und Ebenen` / `Kreise` corridors on the frozen space-geometry surface, `RP` now also adds an explicit GF/LF `Raum und Form` split in which `Geraden und Ebenen im Raum` can stay on the frozen shared space-geometry surface while the parallel matrix/application corridors remain separate enough that no sharper `AGV3-AGV5` bridge is forced at package level, `SL` now also adds a first reviewed upper-secondary geometry corridor in which `G-Kurs` / `Leistungskurs` are split into explicit `Vektoren` and `Vektorielle Untersuchung geometrischer Strukturen` strips on the frozen shared space-geometry surface while the optional `Kreis und Kugel` Pflichtbereich remains outside this first corridor, and `MV` now also adds a first reviewed broad upper-secondary geometry corridor in which the current source still mixes `Vektoren und Matrizen` with `Geraden und Ebenen` on the frozen shared space-geometry surface without forcing a sharper `AGV3-AGV5` bridge, while `TH` now also adds a first reviewed broad upper-secondary geometry corridor in which `Klassenstufe 11: Geometrie` and `Klassenstufen 11/12: Analytische Geometrie` stay together on the frozen shared space-geometry surface without forcing a sharper `AGV3-AGV5` bridge, and remaining tension still sits mainly in source-granularity residue between broad overview parents and the tighter late packages | freeze package churn here and reopen only if another reviewed lane shows a real shared canonical gap |
| Sek II Lineare Algebra / Matrizen | `seed` | `n/s` | `corr` | `n/s` | `n/s` | `corr` | `corr` | `seed` | `corr` | `n/s` | `n/s` | `corr` | `anchor` | `corr` | `n/s` | `n/s` | `corr` | visible `LM2-LM5` packaging is now in place, reviewed `BB`/`BE`/`BY`/`HB`/`HH`/`HE` evidence is stable, the reviewed `NI` projection-with-matrices lane confirms that the late `LM5` branch is a real but sufficiently narrow continuation rather than a sign of missing shared packaging, and `SN` now also confirms the frozen LM surface with an explicit GK matrix strip, `RP` now adds an explicit GF/LF `Raum und Form` split that keeps matrix/application work visibly separate from the parallel geometry corridor while still fitting the frozen `LM2-LM5` surface without forcing a sharper package-level bridge, and `SL` now at least carries a first broad upper-secondary space/matrix-model anchor across `EP`, `GK`, and `LK`, while `MV` now also adds a first reviewed broad upper-secondary matrix corridor in which the mixed `Vektoren und Matrizen` band remains on the frozen shared space/matrix surface without forcing a sharper `LM2-LM5` bridge, and `TH` now also adds a first reviewed broad upper-secondary matrix corridor even though the current explicit source packaging still speaks only broadly about `Klassenstufe 11` geometry and `11/12` analytic geometry, so the same broad Raum-/AGV strip is deliberately reused as a shared matrix bridge instead of forcing an artificial matrix-only split | freeze package churn here and reopen only if another reviewed lane shows a real shared canonical gap |

## Current recommendation

Active next focus:

1. no new top-level package pass
2. switch from residue control to full topic coverage sweeps
3. start the first full sweep with `Sek II Analysis`

Active coordination artifact:

1. `docs/dev/canonical-gymnasium-math-topic-workboard.md`

Supporting audit artifacts:

1. the existing topic audits as support for all-state closure and reopen decisions only

Why:

- the first canonical packaging passes for the current top-level math topics are now in place
- the reviewed upper-secondary pressure tests across `BB`, `BE`, `BW`, `BY`, `HB`, `HH`, `NI`, `NW`, and `SH` do not currently force another top-level package split
- the next gains now come more likely from systematically clearing the remaining `n/s` cells topic by topic than from opening another top-level package split immediately
- for `Sek II Analysis`, the remaining open cells are no longer source-onboarding blockers; `SN`, `ST`, and now also `TH` are corridor-reviewed on the frozen `AN2-AN4` surface

Coverage queue:

1. `Sek II Analysis`
2. `Sek II Stochastik`
3. `Sek II Analytische Geometrie / Vektoren`
4. `Sek II Lineare Algebra / Matrizen`

Current blocker note:

1. `Sek II Analysis` can now advance mainly via later reviewed backlog corridors; `TH` and `MV` are now corridor-reviewed there rather than anchor-only blockers

Definition of done for one topic:

1. the canonical atom inventory is reviewed and, where needed, tightened
2. reviewed state evidence no longer forces obvious new shared atoms
3. every Bundesland cell in that topic row is resolved once, i.e. no `n/s` cells remain
4. state mappings for the resolved states are consistent with the revised canonical cuts
5. state-local leftovers are explicitly marked as `loc` or kept out of the canonical core
