{
  "name": "skillpilot-claude-mcp-apps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-apps.mjs",
    "test": "npm run build && node --test --test-concurrency=1 test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "1.7.4",
    "@modelcontextprotocol/sdk": "1.30.0",
    "katex": "0.16.27"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
