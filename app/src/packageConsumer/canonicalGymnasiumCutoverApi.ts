export const requestCanonicalGymnasiumCutover = async (
  _fetcher: typeof fetch,
  _apiBase: string,
  _skillpilotId: string,
): Promise<Response> => {
  void _fetcher
  void _apiBase
  void _skillpilotId
  throw new Error('Repository curriculum cutover is unavailable in package-consumer mode')
}
