# Curriculum Package Contract v1

This directory is the trusted repository authority for the SkillPilot curriculum-package v1 contract.

DPK-001 established the manifest/profile kernel; DPK-002 adds package-local discovery, offline schema resolution, and a fail-closed readiness vocabulary. DPK-003 freezes the cross-variant semantic proof contract. DPK-004 completes the strict unpacked Mathematik release model: DPK-004a established Runtime closure, and DPK-004b adds authoritative publication evidence. DPK-005a closes package-level semantic bindings, the complete offline schema trust set, the deterministic ZIP32 primitive, and human-review gates. DPK-005b materializes that model as a reproducible real ZIP and validates the finished carrier independently. DPK-006a adds the read-only consumer for an exactly locked, prevalidated content-addressed store and accepts only validator-v2 evidence bound to ZIP, manifest, closure and definition index. DPK-006b completes the external operational contracts and the quarantined, content-addressed provisioner with immutable evidence, explicit CAS activation, and rollback. DPK-006c routes the complete runtime through the active generation; DPK-007 adds the external, hermetic whole-application consumer proof and completes the readiness policy implementation without weakening human publication gates. DPK-007a imports the final missing Mathematik visualization, aligns the Core/registry RDF lanes, and freezes candidate `0.1.0-conformance.3`. DPK-008a defines the closed inner FWU-OWL manifest, package profile and external validation receipt. DPK-008b builds the real Core-first Mathematik package reproducibly, DPK-008c validates the finished carrier independently through all eighteen production gates, and DPK-008d reconstructs a fully valid JSON runtime package in two isolated runs without access to the original JSON or forward exporter. DPK-009 is now the next step and assembles the normative equivalence report and reproducible variant pairs.

- `package-manifest.schema.json` defines the strict internal manifest shape. Every file is bound either to a normalized logical artifact, to a binary resource identity, or explicitly to generated/non-semantic package material.
- `fwu-owl-package-manifest.schema.json`, `fwu-owl-package-profile.schema.json`, `profiles/fwu-owl-v1.profile.json`, and `fwu-owl-package-validation-report.schema.json` define the second variant's strict inner inventory, fixed eight-segment RDF order, derived-only `bundle.nt`, pinned Core/profile/shapes bindings, binary sidecars, reverse-build support, explicit JSON depth/node/byte limits, eighteen validation gates, and byte-identical double-build receipt. The byte-identical FWU Core is correctly declared as OWL Functional Syntax; filename suffix and package-size restrictions apply only to successful receipts so early failures remain truthfully reportable.
- `ontology/skillpilot-fwu-profile-1.0.0.ttl`, `ontology/skillpilot-fwu-shapes-1.0.0.ttl`, and the Core-only XML catalog are repository-controlled, versioned trust roots. The package profile pins their exact bytes; packages only carry byte-identical copies and cannot replace the application ontology, redirect the Core import, or weaken the non-executable SHACL Core constraints. The closed `goalSemanticKind` enumeration uses explicit `xsd:string` literals, matching the compiler's typed RDF literals across SHACL engines.
- `declarations.nt` is compiled from the exact union of the 485 registry-derived application declarations and all 65 declarations in the pinned application ontology, including the generic `sp:fieldState` carrier for a present empty collection and `sp:referenceRole` for typed competency-axis references. Same-kind duplicates produce 510 application declarations. Sixteen explicitly typed Core/external parser-bootstrap properties bring the closed v1 declaration segment to 526 triples, so a standalone RDF-to-OWL parser cannot silently reinterpret data/object properties as annotations before the Core/profile merge.
- The FWU profile also pins the six bootstrap schemas and three global semantic contracts to repository bytes. Its 25-entry offline catalog is constructed deterministically from those bindings and the trusted 22-schema JSON release profile. A valid FWU receipt proves that the validator independently opened the exact source JSON ZIP and compared its manifest identity plus all five semantic contracts; package-local copies are never trust roots.
- `profiles/full-standalone-v1.profile.json` defines the closed JSON release profile, artifact-role cardinalities, exact role-specific payload schemas, the 22-schema offline trust set, five named semantic contracts, redistribution readiness policy, and package-format limits.
- `runtime-catalog.schema.json` defines explicit roots, offered scopes, view resolution, runtime artifacts, resources, capabilities, and package-local dependency references.
- `schema-catalog.schema.json` defines the offline ID-to-file resolver for hash-bound JSON Schemas.
- `profiles/full-standalone-v1.readiness-policy.json` and `package-readiness-report.schema.json` define the blocking checks and machine-readable decision. `package-consumer-smoke-report.schema.json` is the external operational attestation for the exact ZIP, one-package lock generation, policy-pinned runner and helpers, complete assembly and evidence trees, real browser rendering, isolation trace, five poison sentinels, and closed 15-check runtime proof. The consumer opens the exact `.java-version`/`.corretto-version` JDK and launcher with `O_NOFOLLOW` and binds those descriptors into its hidden-checkout sandbox, so an unpinned host Java cannot become a fallback. Python's base runtime and launcher plus the Node and `strace` launchers are also rebound through read-only descriptors at stable sandbox paths before host `/opt` is hidden; the cleared environment retains only a fixed loader path to the rebound Python `lib/` directory for dynamically linked setup-python builds.
- `full-package-validation-report.schema.json`, `installed-package-record.schema.json`, and `active-package-lock.schema.json` define the external package-store trust chain from the exact six-gate validator-v2 receipt through a content-addressed install record to the strictly package-ID-ordered active lock. These are operational schemas, not package-local payload schemas: they deliberately do not extend the 22-schema `full-standalone-v1` trust set or alter package digests and entry counts.
- `compiled-landscape.schema.json`, `composition-view.schema.json`, `composition-view-index.schema.json`, `card-deck.schema.json`, `card-index.schema.json`, and `resource-index.schema.json` close the runtime payload shapes compiled in DPK-004a.
- `dependency-closure.schema.json`, `embedded-goal-dependency.schema.json`, `definition-digest-profile.schema.json`, and `profiles/canonical-definition-record-v1.profile.json` define typed identity, canonical definition digests, ownership, embedded fragments, fixed-point hard-reference closure, and deterministic conflict handling.
- `migration-aliases.schema.json` defines `renamed`, `replacedBy`, `splitInto`, `mergedInto`, and `removed` relations with explicit, fail-closed mastery and immutable-history policies.
- `field-semantics-registry.schema.json` and `profiles/skillpilot-fwu-field-semantics-v1.registry.json` define closed, reversible JSON-field-to-RDF semantics and dependency behavior. The 454 entries comprise 323 Runtime rules, 130 publication-normal-form rules, and one legacy generic rule. DPK-007a adds an executable alignment gate between those declared lanes and the ontology/publication profiles; the registry itself remains unchanged at SHA-256 `2e536c3f8d63e2acf45690375ace69ec0c6a6e92787bc8a16957b80120c4ca48`.
- `semantic-normalization-profile.schema.json`, `profiles/semantic-normal-form-v1.profile.json`, and `semantic-content-index.schema.json` define canonical JSON, ordered-versus-set behavior, missing/null/default handling, binary-resource records, length framing, and the packaging-neutral `contentDigest` input.
- `curriculum-ontology-profile.schema.json`, `profiles/de-gymnasium-mathematik-v1.profile.json`, and the fingerprinted Mathematik semantic-kind ledger bind Core-first mapping decisions to an exact FWU repository, commit, source file, profile hash, and namespace-map hash without title, ID, or path inference. Compiler and independent validator also prove that every mapped Core term occurs in the pinned OWL bytes.
- `release-model-build-profile.schema.json` and `profiles/de-gymnasium-mathematik-release-model-v1.profile.json` bind the real canonical landscape, 88 views, 12 decks, 826 resource links, path relocations, trusted contracts, and exact conformance counts.
- `publication-evidence-projection.schema.json` and `profiles/de-gymnasium-mathematik-publication-evidence-v1.profile.json` classify authored mapping, source, and review inputs and bind four generic package roles to the specific normalization roles `source-to-canonical-mappings`, `official-source-index`, `source-goal-reference-index`, and `release-quality-evidence`.
- `source-to-canonical-mappings.schema.json`, `official-source-index.schema.json`, `source-goal-reference-index.schema.json`, and `release-quality-evidence.schema.json` close those four release-facing payloads. They contribute normalized records to the shared `contentDigest` but declare `runtimeRequired: false` and never enter dependency closure.
- `package-redistribution-review.schema.json` and the Mathematik redistribution ledger bind every embedded image and every non-binary artifact class to provenance and an explicit redistribution decision. Provider labels never become licenses; unresolved decisions use `licenseExpression: null` and block readiness without making the manifest structurally invalid.
- `source-verification-review.schema.json` and the Mathematik source-verification ledger distinguish reproducible machine text matches from human review decisions. No machine match counts as a human approval, and no extracted official-PDF text projection is committed.
- `fixtures/full-standalone-package/` and `scripts/validate_full_standalone_curriculum_package.py` exercise an implementation-independent finished-ZIP validator. It verifies physical archive safety, exact inventory, package-local Runtime/schema catalogs, hard-reference closure, semantic digest, and binary-resource bytes without importing Builder code.
- `fixtures/package-provisioner/` binds one valid report/install/lock byte chain and exercises closed fields, exact protocol and gate sets, hash/release/binding substitution, duplicate and unsorted package selections, duplicate JSON keys, and trailing tokens without weakening the existing manifest fixture suite.
- `equivalence-report.schema.json` and `dual-release-index.schema.json` bind the isolated reverse compilation, field coverage, graph/view/card/asset parity, ontology gates, hermetic consumer tests, reproducibility, final ZIPs, provenance, and release authentication.
- `fwu-owl-reverse-compilation-report.schema.json` and `fixtures/fwu-owl-reverse-compilation/` define one complete valid receipt plus truthful early `invalid` and `error` states. A valid report binds the exact FWU ZIP and its clean eighteen-gate receipt, reverse compiler, hermetic runner, sandbox entry, independent JSON package validator, `bubblewrap`, `strace`, the minimal Python Runtime closure, both output ZIPs, both validator receipts, raw and filtered traces with their process-tree manifests, the exact twenty-file evidence inventory, closed isolation claims, and byte-identical reproducibility.
- `fixtures/valid/` contains conforming manifest examples.
- `fixtures/` contains positive, mutation-based, raw-JSON, cross-binding, adversarial ZIP, report-forgery, dual-release, and release-model ownership/fixed-point contract cases.
- `fixtures/fwu-owl-package/` and `scripts/validate_curriculum_fwu_owl_package_contracts.py` exercise one externally anchored contract set, truthful early invalid/error receipts, 96 fail-closed semantic mutations, duplicate-key rejection, and non-finite-number rejection independently of the exporter. The dual-release validator adds 61 cross-variant mutations.
- `scripts/validate_fwu_owl_curriculum_package.py` is the exporter-independent finished-package validator. It opens source and FWU ZIPs through stable `O_NOFOLLOW` descriptors, rederives all archive, manifest, RDF, registry, Core/profile, JSON-sidecar, binary-resource, source-package and reproducibility bindings, and persists separately hash-bound RDF, SHACL, OWL 2 DL and HermiT evidence. Its self-test fixes 40 fail-closed guarantees without importing the TypeScript exporter.
- `scripts/reconstruct_json_curriculum_package_from_fwu_owl.py` is the standalone reverse compiler. It streams the asserted RDF segments into SQLite, inverts the exact embedded 454-entry registry, recovers all 111 normalized logical artifacts, regenerates only package mechanics, and copies embedded resources only after exact byte checks. Its self-test fixes 31 guarantees.
- `scripts/run_fwu_owl_reverse_compiler_hermetic.py` and `scripts/fwu_owl_reverse_compiler_sandbox_entry.py` execute two descriptor-bound `bubblewrap --clearenv` builds in a host-root-free minimal namespace. The pinned Python runs with `-I -S`; only narrow root-owned Python and native-library roots are mounted read-only, while the host Rawtrace derives and rehashes the exact files actually opened, including ELF loader dependencies. Networking is unshared, and a host-side `strace` process remains invisible to sandbox `/proc`. Each output must pass the independent full-package validator v2 and both archives must be byte-identical. Its self-test fixes 21 guarantees.
- `scripts/validate_curriculum_fwu_owl_reverse_compilation_contract.py` validates both report structure and the bytes referenced by a real report. It parses the nested eighteen-gate FWU receipt, both six-gate validator-v2 receipts, compiler reports and probes, reopens ZIP central directories, manifests, `SHA256SUMS` and semantic indexes, independently rederives the Entry/Core trace from the host Rawtrace, and derives the exact Runtime closure including Python and tracer ELF dependencies. Its fixture suite contains one valid and two truthful early receipts, 53 semantic mutations, 6 raw-JSON rejection cases, and 10 attacks whose forged evidence is consistently rehashed.

The DPK-004 compiler emits an unpacked conformance model below `tmp/`. The immutable DPK-007a candidate `0.1.0-conformance.3` uses ontology and publication profiles `1.1.1` and contains 111 logical and 757 binary content records with digest `sha256:e83936aaf3645ff5f6e8132c4a801bd4bd66f55d3c0304a5deda3d6a5d194101`. Its Runtime closure has 2,403 definitions and 18,820 references, definition-index digest `sha256:4e99bba1d71d26b94bc23f4ea8251ff4dd3df15a5c63006b7564f7f69948c57d`, closure digest `sha256:7e7d704a9c5e17fbe24f6ac881b44b41ae930f5ac945e69738b753c98b999121`, and no publication roles. The real `full-standalone-v1` staging ZIP has 914 entries, of which 912 are manifest-inventoried and 757 are binary image assets totaling 1,696,390,279 bytes. The ZIP `skillpilot-curriculum-de-gymnasium-mathematik-0.1.0-conformance.3.json.zip` is 1,738,161,217 bytes with SHA-256 `403cc0bc6004da549c8b9ed9fafad222fe0ddda1107806fe087cfa871a6dbcf9`; its manifest SHA-256 is `32f732fc553fd39a462280eba7b2fa94367af34b3882ec6115a94948da4b1ebe`. The Builder's one public ZIP operation performs two differently ordered builds and atomically promotes only byte-identical output.

The finished DPK-008c FWU-OWL candidate has 819 ZIP entries and 817 manifest-inventoried files. The DPK-008d reverse oracle exposed and fixed a Placement-IRI collision: identities now use `goalId@unitId@index`, preserving repeated placements of the same goal and unit. The corrected byte-identical primary and peer archives are each 2,362,455,128 bytes with SHA-256 `abab1d8aac3e9394af26c614bbf231954ba45ab11f725dd0f93f088820dc3f94`; the inner manifest SHA-256 is `29f308424d1aeba9095f0e800253acadcdfaca0562dfa1fc37741c77c76023b3`. Eight ordered RDF segments contain 824,452 triples while retaining the JSON candidate's exact semantic `contentDigest`. The production validator passes all 18 ordered gates, including zero-violation SHACL, OWL 2 DL, HermiT consistency with no unsatisfiable named classes, source-JSON rebinding, byte-identical assets and FWU reproducibility.

The isolated reverse compiler produces a 1,737,571,471-byte JSON ZIP with SHA-256 `7dcd233dd495900f6d6bd971ff6e86bdcdcfe5701f3522ed480be8336de23195` and manifest SHA-256 `8d7970435431ff78743d0bf413a54bbebee009bbaafe35f1283ebcb89b4f2ff0`. It contains 911 ZIP entries, 909 manifest-inventoried files, 910 checksum rows, all 111 logical artifacts, and 757 binary resources totaling 1,696,390,279 bytes. All 111 normalized payload hashes and every binary byte binding match the FWU content index. Both private builds are byte-identical and independently valid `full-standalone-v1` packages.

The publication projection treats the 10,021 reviewed mapping decisions and their 33,382 exact/partial edges as truth. An explicit decision match type wins; otherwise the compiler uses the edge-specific reviewed mapping value, never an implicit `exact` default. This resolves 2,599 edges from reviewed edge rows with zero unresolved types. Those rows contain 33,334 edges, with 48 decision edges absent and 23 explicit decision-versus-row conflicts. Legacy mapping files remain quarantined authoring/audit compatibility data: their 2,539 rows include 695 source IDs outside historical membership and eight dangling targets. Official evidence covers 31 collections, 55 documents, 9,977 source goals, and 16 jurisdictions. Machine source verification proves 9,493 contiguous authored-carrier matches and five additional hash-bound PDF-projection matches; 479 records remain explicit human work. Quality publishes 2,303 current decisions across a 754-goal atomic visualization scope. There are 731 active in-scope visualizations and 23 goals deliberately deferred after provider-limited attempts; 151 active visualizations are human-approved and 580 active reviews keep `publicationStatus` honestly at `not-ready`. Redistribution is independently blocked by 734 package-image decisions and three non-binary class decisions awaiting human review; six image records carry a user-provided generation claim, which is provenance rather than a rights grant.

The existing subject exporter still produces the explicitly marked legacy subject-export format. It is not conformant with `full-standalone-v1`; successful legacy export and roundtrip checks remain `not-ready-legacy`. For the new staging ZIP, inventory, Runtime catalog, offline schema catalog, hard-reference closure, content digest and binary bytes are implemented and independently checked. DPK-006a additionally proves exact read-only loading from a prevalidated store, DPK-006b/006c supply safe provisioning and package-authoritative services, DPK-007 proves the hermetic whole-application consumer path, and DPK-007a freezes its complete content and Core/registry lane semantics. DPK-008a–d define, build, independently validate, and reverse-compile the real FWU-OWL variant. The current authoring projection nevertheless remains `not-ready-incomplete`, because 580 active fachliche image reviews, 23 provider-deferred visualization gaps, 737 redistribution reviews, and 479 source-text reviews remain open gates. The inner package-manifest and `full-standalone-v1` profile cover only the JSON runtime variant; the external DPK-003 proof contracts cover both variants. DPK-009 next compares original and reconstructed packages as post-build oracles, runs both consumers, and emits the normative equivalence and dual-release receipts.

Run the contract conformance gate from the repository root:

```bash
python3 -B scripts/validate_curriculum_package_contracts.py
python3 -B scripts/validate_curriculum_fwu_owl_package_contracts.py
python3 -B scripts/validate_curriculum_runtime_catalog_contract.py
python3 -B scripts/validate_curriculum_schema_catalog_contract.py
python3 -B scripts/evaluate_curriculum_package_readiness.py --self-test
python3 -B scripts/validate_full_standalone_curriculum_package.py --self-test
python3 -B scripts/run_package_consumer_smoke.py --self-test
python3 -B scripts/validate_fwu_owl_curriculum_package.py --self-test
python3 -B scripts/reconstruct_json_curriculum_package_from_fwu_owl.py --self-test
python3 -B scripts/run_fwu_owl_reverse_compiler_hermetic.py --self-test
python3 -B scripts/validate_curriculum_fwu_owl_reverse_compilation_contract.py --self-test
python3 -B scripts/validate_curriculum_dual_release_contracts.py
python3 -B scripts/generate_curriculum_package_redistribution_review.py --check
python3 -B scripts/generate_curriculum_source_verification_review.py --check
```

Provision the pinned ontology tools once, then run the heavyweight complete FWU-OWL and reverse gate:

```bash
python3 -m pip install -r scripts/curriculum_fwu_owl_validation_requirements.txt
bash scripts/provision_pinned_robot.sh
python3 -B scripts/check_curriculum_fwu_owl_validation_tools.py \
  --report tmp/curriculum-release-model/fwu-owl-validation/tools-report.json
bash scripts/run_curriculum_fwu_owl_reverse_conformance.sh
```

Only provisioning may use the network. The conformance run checks exact Python dependency, ROBOT 1.9.10, HermiT 1.4.5.456 and Amazon Corretto 25.0.2.10.1 identities before validating offline. It writes the canonical FWU receipt to `tmp/curriculum-release-model/fwu-owl-validation/fwu-owl-package-validation-report.json` and the reverse receipt to `tmp/curriculum-release-model/fwu-owl-reverse/reverse-compilation-report.json`. The reverse wrapper includes the complete `scripts/run_curriculum_fwu_owl_package_conformance.sh` gate before starting its two isolated builds; run that inner script directly only when the carrier without reconstruction is the intended scope. These multi-gigabyte gates are intentionally separate from ordinary CI; CI runs bounded 40-finished-package-validator, 31-reverse-compiler, 21-hermetic-runner, and 53+6+10 reverse-contract self-tests and verifies the pinned toolchain contract.

Compile the real unpacked Mathematik model into a disposable directory:

```bash
python3 -B scripts/compile_curriculum_release_model.py \
  --profile contracts/curriculum-package/v1/profiles/de-gymnasium-mathematik-release-model-v1.profile.json \
  --output tmp/curriculum-release-model/mathematik

python3 -B scripts/validate_curriculum_release_model.py \
  --profile contracts/curriculum-package/v1/profiles/de-gymnasium-mathematik-release-model-v1.profile.json \
  --release-root tmp/curriculum-release-model/mathematik
```

Materialize and validate the real JSON staging package from that model:

```bash
npm --prefix app run export:full-standalone-package -- \
  --release-root tmp/curriculum-release-model/mathematik \
  --output-dir tmp/curriculum-release-model/full-standalone-package \
  --zip \
  --expect-entry-count 893 \
  --expect-manifest-file-count 891 \
  --expect-binary-asset-count 734 \
  --expect-content-digest sha256:830b0e3b98b5ade7a4efc412fd39feb378e98302367fe57fa6c49e23e50a28c4

python3 -B scripts/validate_full_standalone_curriculum_package.py \
  --zip tmp/curriculum-release-model/full-standalone-package/skillpilot-curriculum-de-gymnasium-mathematik-0.1.0-conformance.3.json.zip \
  --report tmp/curriculum-release-model/full-standalone-package/full-package-validation-report.json

python3 -B scripts/evaluate_curriculum_package_readiness.py \
  --zip tmp/curriculum-release-model/full-standalone-package/skillpilot-curriculum-de-gymnasium-mathematik-0.1.0-conformance.3.json.zip \
  --consumer-smoke-store tmp/curriculum-release-model/provisioned-store \
  --consumer-smoke-work-dir tmp/curriculum-release-model/package-consumer-smoke \
  --consumer-smoke-report tmp/curriculum-release-model/full-standalone-package/package-consumer-smoke-report.json \
  --report tmp/curriculum-release-model/full-standalone-package/readiness-report.json \
  --expect-status not-ready-incomplete
```

The example assumes that the exact ZIP has already been securely provisioned and activated in the named store; the focused wrapper below performs that complete sequence.

The focused gate `bash scripts/run_curriculum_release_model_conformance.sh` validates 9 positive fixture documents and 22 targeted negatives, runs the compiler dependency-emission probe and 47 production/adversarial cases, rejects destructive output-symlink and non-regular-node cases, verifies the pinned FWU Core checkout, performs the independent real-model validation, and produces a second byte-identical model build. It then invokes the real ZIP Builder exactly once—its ZIP operation is internally a double build—writes an independent validator-v2 report and securely provisions and activates the exact package. The readiness evaluator verifies and invokes the exact policy-pinned runner itself, captures a fresh private report from the package-only React/Chromium consumer, independently re-digests the final assembly and evidence trees, and only then atomically persists both reports. A pre-existing consumer report supplied without `--consumer-smoke-store` remains `external-unattested` and cannot satisfy the consumer gate. The required result remains honestly `not-ready-incomplete`: consumer operability passes while open redistribution remains blocking.

The compiler accepts output directories only below repository `tmp/`. Its output and validation workflow are documented in [Curriculum Release Model Conformance](../../../docs/qa-ci/curriculum-release-model-conformance.md).

Use `--verbose` on the catalog validators to print every fixture result. All validators require the pinned `jsonschema==4.26.0` runtime and fail closed when trusted contracts are inconsistent, a binding is stale, or a valid fixture fails. The manifest/catalog suites assert their exact expected diagnostics; the DPK-003 adversarial matrix requires the named invariant diagnostic even when one mutation necessarily triggers additional dependent failures.

## Trust and inventory rules

The manifest binds the manifest schema, release profile, runtime-catalog schema, and schema-catalog schema by ID, archive-root-relative package path, and SHA-256. The validator requires each bootstrap declaration to match both the trusted file in this directory and exactly one corresponding `files[]` record. The release profile carries the closed trusted-schema registry. A package-local contract copy is therefore useful for offline processing but cannot redefine the trusted contract.

`validationSchemaId` links every normative JSON role to its exact allowed payload schema. The trusted profile pins the complete 22-schema set used by the Runtime and semantic contracts; the offline schema catalog must contain that complete set and bind each entry one-to-one to a manifest record with role `schema`, including path, media type, byte length, and SHA-256. Resolution uses only the in-memory package registry: remote retrieval, ambiguous URI forms, missing references, unknown schema files, and package-controlled replacement of a trusted schema are rejected.

All normal files are inventoried through explicit artifact roles. `metadata/manifest.json` is excluded because a manifest cannot hash itself; `metadata/SHA256SUMS` is generated after the manifest and binds the manifest together with the payload. Paths in `files[]` never contain the archive-root prefix.

Package roles and normalization roles are intentionally distinct. Generic roles such as `mapping`, `source-index`, `source-goal-reference-index`, and `quality-evidence` are the stable manifest/consumer vocabulary. The trusted publication profile binds each generic role one-to-one to its specific `normalizationRole`, closed schema, and field-registry lane. The content index records the generic role, while canonicalization uses the bound normalization role; an unknown, duplicated, or mismatched binding fails closed.

All contract JSON is parsed with duplicate-object-key rejection before schema validation, avoiding parser-dependent interpretations across Python, Java, and TypeScript. The v1 profile explicitly caps every JSON entry at 64 MiB, nesting at 128 levels, and the parsed tree at 5,000,000 nodes; the finished-ZIP validator reads those values from the trusted profile and has no hidden stricter JSON limit. The profile also caps a manifest at 64 MiB, 59,998 inventoried file records, and 1,024 license documents; array limits fail fast before full schema traversal, and path lookups are indexed rather than quadratic.

Every identifier used in a file's SPDX-style `licenseExpression` is resolved through `licenseDocuments` to an inventoried `license` artifact. `NONE`, `NOASSERTION`, malformed expressions, unresolved identifiers, and orphaned license texts fail the public standalone profile. Provenance remains a separate field and never substitutes for a redistribution license. `review-required` and `prohibited` records use `null`, remain structurally representable for staging, and fail the independent `publication.redistribution-cleared` readiness check.

The role table in the profile is authoritative. Runtime catalog, dependency closure, migration aliases, semantic content index, canonical landscape, view index/views, card index, resource index, schema catalog, schema set, five named semantic contracts, release profile, and license are required. Optional roles remain closed and explicitly enumerated; unknown roles fail validation. The runtime catalog must agree exactly with the manifest's runtime-role path sets, even when a set is empty, and every selectable root must resolve through an explicit default offering to an available composition view.

The profile also records the approved manifest/ZIP32 limits, portable full-path cap, and maximum per-entry/overall compression ratios. Manifest-level checks cover declared entry counts and sizes, image-lane limits, and nested-archive prohibition. The real finished-ZIP validator additionally enforces the raw manifest-byte limit before parsing and verifies outer ZIP size, compressed sizes, ratios, local/central header coherence, actual decompressed metadata and payload hashes instead of trusting manifest declarations.

## Cross-variant semantics and proof

The DPK-003 registry rejects unknown fields and separates `ordered-list`, `set`, `scalar`, `map`, `binary-reference`, and generated non-semantic data. Runtime-observable lists such as goals, direct `contains` and `requires` edges, resource links, composition children, cards, and scoring steps retain explicit zero-based RDF positions. A Core projection such as BFO parthood or `LP_0000554` prerequisites is additive and never replaces the positioned authored edge.

Canonical JSON literals are limited to registered owner subtrees with a stated Core gap and byte cap. They cannot carry a whole package or landscape. Known curricular text and prerequisite semantics use the FWU Core; application vocabulary remains limited to actual runtime, reconstruction, ordering, view, card, assessment, and packaging concerns.

The production reverse oracle fixes several edge semantics that implementations must preserve. Goal placements have the collision-free identity `goalId@unitId@index`; `unitId`, not a guessed `programUnitId`, is the authored unit key. Registry-typed finite decimals, explicit `null`, missing fields, and strings remain distinct. Resources with `delivery: external` intentionally remain without `artifactPath`, while embedded resources require an exact sidecar binding. Dependency closure recovers all variable logical-artifact paths; closed singleton roles use their standard paths, allowing only an identical redundant closure binding and rejecting any conflict.

The semantic content index contains no variant ZIP paths. It binds each normalized logical artifact's generic package role, stable ID, media type, length, canonical-payload SHA-256 and derived record SHA-256, plus stable binary-resource identity, canonical public reference, MIME type, length, file digest, and independently recomputed record digest. The checked fixture proves that a deterministic length-framed index yields one shared content digest. DPK-004 supplies the real Mathematik compiler and independent validator for Runtime and publication bytes; DPK-005b now turns the completed directory into the independently checked staging package.

The dual-release fixtures are conformance examples, not evidence for a real curriculum release. In particular, their dummy ZIP, Core, tool, and ontology-profile hashes do not make the current legacy subject export ready. A report may claim `passed` only with exact registry coverage, three content-index-bound normal forms, ordered graph/view/card parity, byte-identical assets, package-bound ontology checks, an isolated reverse compiler, independently validated original/reconstructed JSON packages, two hermetic consumer tests, and reproducible JSON and FWU-OWL ZIPs. The release contract defines a non-self-referential signing projection. Until DPK-011 supplies cryptographic signature and provenance verification, this validator rejects every `verified` claim and every `stable` promotion; the positive fixture is explicitly unsigned staging.
