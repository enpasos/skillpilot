// CI runner policy, also used by Playwright's nested apt-get invocations.
// Retain the image's Ubuntu mirrors, suites (including security) and Signed-By.
Dir::Etc::sourcelist "sources.list.d/ubuntu.sources";
Dir::Etc::sourceparts "-";
