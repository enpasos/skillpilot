# Exact Python dependency used by the JSON curriculum-package validators.
# The optional FWU-OWL suite installs the larger RDF/SHACL toolchain from
# curriculum_fwu_owl_validation_requirements.txt instead.
jsonschema==4.26.0
